# DamageStats 项目介绍

## 项目用途

DamageStats 是运行在 Minecraft 1.21.1 与 NeoForge 21.1.233 上的服务端权威伤害统计模组。它记录 `LivingEntity` 的有效受伤事件，按责任来源、直接来源、目标和伤害类型提供精确统计，并通过全屏 GUI 与 Overlay 展示当前玩家的焦点数据。

客户端和服务端都必须安装本模组，且协议版本必须一致。客户端不自行计算统计结果，所有采集、权限判定、筛选、排序和分页均由服务端完成。

## 伤害模型

每条有效事件来自 `LivingDamageEvent.Post`。最终伤害不大于零的完全免疫或完全格挡事件不会进入统计。

| 概念 | 语义 |
|---|---|
| 责任来源 | 对伤害负责的实体；投射物归属射手，驯服生物归属主人。 |
| 直接来源 | 实际接触目标的实体，例如箭、火球、狼或近战玩家。 |
| 目标 | 承受伤害的 `LivingEntity`。 |
| 环境 | 无实体来源的虚拟责任来源，涵盖摔落、岩浆、缺氧等。 |
| 原始伤害 | 减免计算前的伤害。 |
| 最终伤害 | `LivingDamageEvent.Post#getNewDamage()` 的最终值，保留致死过量部分。 |

事件还保存格挡量、护甲、附魔、药水、吸收、天生抗性、无敌帧六项减免数据、游戏刻、现实时间、目标维度与坐标。这些数据可用于实例辨识、详细指标和导出。

## 数据生命周期

完整原始事件存放在 `<世界目录>/damagestats/events/`：

| 文件 | 行为 |
|---|---|
| `segment-<序号>.bin` | 每段最多 512 条追加式二进制事件，单帧有长度与 CRC32 校验。 |
| `segment-<序号>.bin.gz` | 已封存段的压缩形式，用于降低长期存储量。 |
| `index.bin` | 来源、目标、直接来源和伤害类型的段级索引；损坏或缺失后可从段文件重建。 |
| `resets.bin` | “清空我的”实例可见性边界，不删除其他查询仍需要的共享事件。 |

活跃段每 100 tick 批量落盘，正常停服时封存；刷新、压缩或索引失败时不会把日志标记为已关闭，并会在下一次刷新或停服收尾时重试。异常关闭后的尾部半帧或校验失败帧会被忽略，历史中此前完整的帧仍可读取。旧 JSONL 事件段首次打开时按行迁移，损坏行只跳过并记录行号，其余有效记录继续导入，原文件重命名为 `.migrated` 留供核对。

`stats.json` 是聚合与实例目录缓存，不是组合查询的权威来源。`autoSave` 仅控制定时写出该缓存；正常停止、重置和启动恢复仍会维护缓存。若缓存损坏，服务端会从完整事件日志回放聚合和实例目录后修复缓存；历史名称无法从不含名称的事件帧推导时，界面回退到玩家或实体类型名称。

权限等级 2 的管理员可通过 `/damagestats storage` 打开服务器存储概览，查看事件数、分段数、原始段与压缩段占用、索引状态、缓存、服务器导出、临时文件和迁移备份占用。概览中的清理均由管理员手动触发并要求二次确认：临时文件/迁移备份、服务器导出文件和全部记录可分别清理。清理全部记录会同步清空事件日志、索引、聚合缓存和实例目录；此后精确查询以清理后的事件历史为准。客户端本地导出文件不受服务器管理。

## 焦点与同步

每位玩家有一份私有焦点，焦点只由来源和目标构成，可分别是实体类型、具体实例或不限。默认焦点为“当前玩家作为来源，目标不限”。页头与 Overlay 始终展示同一焦点。

焦点切换、登入时会从完整事件日志建立一次摘要缓存。之后发生伤害时，服务端先使用来源、目标、直接来源订阅索引锁定候选玩家，再用完整筛选复核；同一服务器 tick 的多条匹配伤害合并成一份摘要增量，在该 tick 结束前发送。非焦点事件不会推送统计数据，图表和实例列表也不会被后台自动刷新。

摘要包含本场与累计两种范围的原始/最终伤害、减免、命中、平均与实时 DPS、平均单次、最高伤害、最高贡献伤害类型、最高贡献直接来源及会话状态。会话超时后仅更新实际超时的焦点。

## GUI 行为

按 `H` 打开全屏统计界面。界面由固定页头、可滚动图表区和固定操作区组成：

1. 页头选择当前浏览的来源与目标，并显示与 Overlay 同源的焦点摘要。本场和累计摘要同时显示。
2. 浏览条件不等于当前焦点时，用户必须点击“设为焦点”；仅浏览不会改变 Overlay。
3. 图表区按最终伤害降序显示横向直方图。可按伤害类型分类、伤害类型注册表 ID、直接来源、责任来源或目标拆分。图表、搜索和实例候选均由服务端分页，每页最多 50 项。
4. `LivingEntity` 图表行可以设为来源或目标；类型行需要先打开实例列表，不能冒充某一具体实体。
5. 历史页从完整事件日志按会话超时边界重建最近已结束的会话摘要。详细页显示原始/最终伤害、DPS、命中、最高最低伤害和六项减免。
6. 重置操作始终二次确认。个人重置只影响当前玩家可清除的实例范围；管理员可以执行全局重置。
7. 导出当前浏览筛选时，服务端会复核浏览授权，并分块传输本场与累计的全部伤害类型、直接来源、对手分组和会话历史；客户端收齐后写入本地导出文件。

## 权限模型

普通玩家的页头来源固定为自己，可以选择目标类型或已记录目标实例。普通玩家唯一额外的来源入口是：在自身输出的直接来源图表中选择一个确实出现在自身伤害链路里的 `LivingEntity` 具体实例，例如自己的狼。服务端会基于完整事件日志证明这条链路；切换目标、关闭 GUI 或断线后授权失效并恢复为自身来源。

权限等级 2 的管理员可以选择任意已记录的 `LivingEntity` 类型或实例作为来源，也可以清空来源查询目标承受的全部伤害或世界全局伤害。`publicStats=false` 时，普通玩家的查询必须涉及自己或合法下钻实例。

## 实例目录

实例选择不会扫描当前世界加载的实体，而是使用已记录实例目录。目录保存 UUID、实体类型、玩家名或自定义名、最近交互的游戏刻、维度、坐标与现实时间。每个世界最多保存 `instanceDirectoryLimit` 个候选，默认 5000；候选保留 `instanceDirectoryRetentionHours` 小时，默认 72。淘汰实例目录不会删除完整事件历史。

目标类型选择会列出注册表中全部 `LivingEntity` 类型，支持显示名和注册表 ID 搜索；类型选中后可继续选择具体已记录实例。

## Overlay

按 `J` 开关 Overlay，默认关闭且只展示、不拦截输入。Overlay 设置从统计 GUI 进入，可以选择任意目标、实体类型或实例；来源固定为当前玩家，修改目标会修改唯一焦点。

客户端配置支持位置比例、缩放、背景透明度和逐项字段勾选。原始与最终伤害、平均 DPS、实时 DPS 等字段可分别选择本场或累计范围。最高伤害的折前与折后是独立选项；同时启用时显示为“最高伤害：折前 / 折后”。渲染会按可用屏幕区域裁剪长文本和过多字段。

## 配置与入口

服务端配置包括采集开关、会话超时、实时 DPS 窗口、实例目录保留期与上限、历史会话数量、定时缓存快照和公开统计。伤害类型分类位于 `<配置目录>/damagestats-damage-types.json`，将英文分类名映射到伤害类型 ID；未归类伤害始终可按原始注册表 ID 查询。

常用入口：

| 入口 | 行为 |
|---|---|
| `H` | 打开统计 GUI。 |
| `J` | 开关 Overlay。 |
| `/damagestats toggle` | 切换伤害采集，需要权限等级 2。 |
| `/damagestats reload` | 重载伤害类型分类，需要权限等级 2。 |
| `/damagestats export` | 导出服务器统计，需要权限等级 2。 |
| `/damagestats storage` | 打开服务器存储概览，需要权限等级 2。 |

服务端命令导出到 `<世界目录>/damagestats/exports/`。GUI 导出当前浏览筛选的完整结果到 `<游戏目录>/damagestats/exports/`，不需要额外权限，但仍受服务端浏览授权约束。两种导出都会生成同一时间戳的 JSON 与 CSV；JSON 包含指标、筛选、本场与累计各三类分组、会话历史和六项减免明细，CSV 包含全部分组明细。

## 当前待完成事项

当前不要求单元测试或 GameTest；仍需在单人、多人权限和不同界面尺寸下完成游戏内人工验收，覆盖焦点增量、实例下钻授权、分页、GUI 导出分块、事件索引重建、尾帧损坏恢复、长期高频伤害性能和渲染。
