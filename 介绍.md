# DamageStats 项目介绍

## 1. 定位

DamageStats 是 NeoForge 1.21.1 的服务端权威伤害统计模组。它记录 `LivingEntity` 的有效伤害事件，将数据按来源、目标、直接来源和伤害类型聚合，并提供以下能力：

1. 自动采集玩家、怪物、投射物、驯服生物和环境造成的伤害。
2. 同时维护正在进行的战斗会话和跨存档运行周期的永久累计。
3. 在 GUI 中按实例、实体类型、直接来源和伤害类型筛选统计。
4. 通过可拖拽 Overlay 显示当前玩家的输出摘要。
5. 按世界隔离持久化，并导出 JSON 与 CSV。

它不是客户端本地统计工具。客户端和服务端都必须安装相同版本的模组，服务端是所有统计数据的唯一权威来源。

## 2. 运行基线

| 项目 | 值 |
|---|---|
| Minecraft | 1.21.1 |
| NeoForge | 21.1.233 |
| Java | 21 |
| 映射 | Parchment 2024.11.17 |
| 模组 ID | `damagestats` |
| Java 根包 | `io.zershyan.damagestats` |
| 当前版本 | `0.0.1-alpha` |
| Mixin | 当前未使用 |
| KubeJS | 当前不提供接口 |

工程使用 Gradle 的 NeoForge ModDev 插件。语言文件由数据生成任务产生，翻译声明仅维护在 `DSKeyLang` 和 `DSConfigLang` 中；不应直接修改 `src/generated/resources/assets/damagestats/lang/*.json`。

## 3. 核心术语

| 名称 | 含义 |
|---|---|
| 责任来源 | 这笔伤害最终归属的实体。箭伤害归属射手，驯服生物伤害归属主人。 |
| 直接来源 | 直接接触目标的实体。箭、火球、狼或近战玩家本身都可能是直接来源。 |
| 目标 | 受到伤害的 `LivingEntity`。 |
| 环境 | 没有实体来源时使用的虚拟来源，拥有固定 UUID。 |
| 原始伤害 | 进入减免前的数值。 |
| 实际伤害 | `LivingDamageEvent.Post#getNewDamage()` 返回的最终伤害值。它包含致死过量伤害，因此不等同于目标生命值实际减少量。 |
| 战斗会话 | 连续有有效伤害的时间段，超时后自动结束。 |
| 永久累计 | 当前世界内持续累积的数据，只有显式重置才会清零。 |

## 4. 伤害采集流程

采集入口是 `handler/common/DamageCollectHandler`：

```text
LivingDamageEvent.Post
        |
        +-- 最终伤害 <= 0：直接丢弃
        |
        +-- DamageResolver 解析责任来源、直接来源和伤害类型 ID
        |
        +-- 构造 DamageRecord
        |
        +-- DamageTracker 同时写入原始日志、来源统计、目标统计、类型统计和全局统计
```

该入口选择 `Post` 的原因是伤害结算已定稿，并且能够同时取得：

1. 原始伤害 `getOriginalDamage()`。
2. 最终伤害 `getNewDamage()`。
3. 格挡量 `getBlockedDamage()`。
4. 六项减免量：护甲、护甲附魔、药水效果、伤害吸收、天生抗性和无敌帧。

完全免疫、完全格挡或任何最终伤害不大于零的事件不会进入统计。这些事件不增加命中次数，也不会进入筛选日志。

### 4.1 来源解析

`DamageResolver` 先读取 `DamageSource` 的责任实体，再沿 `OwnableEntity` 的主人链向上最多追溯四层。因此投射物和驯服生物的伤害会记入对应主人；直接来源始终保留 `DamageSource#getDirectEntity()`。没有实体来源时使用 `EntityRef.ENVIRONMENT`，不会丢弃环境伤害。

伤害类型取 `DamageSource#typeHolder()` 的注册表键。无法取得注册表键的第三方构造类型回退为 `damagestats:unknown`，保证记录仍可进入统计。

## 5. 数据模型与聚合

### 5.1 原始记录

`DamageRecord` 包含以下字段：

| 字段 | 用途 |
|---|---|
| 责任来源、直接来源、目标 | 每个均保存 UUID 与实体类型，用于筛选与显示。 |
| 伤害类型 ID | 用于类型分解、分类显示与精确筛选。 |
| 原始伤害、最终伤害、格挡量 | 计算总量、减免和命中指标。 |
| 六项减免明细 | 用于完整导出和持久化。 |
| 游戏刻 | 用于会话切分、平均 DPS、实时 DPS 和最高单次伤害时间。 |
| 致死标记 | 用于击杀数。 |

`DamageLog` 是服务端内存中的环形日志，默认保存 20000 条、最大 500000 条。它不写入存档。日志淘汰过旧记录后，组合筛选快照会标记“数据不完整”。

### 5.2 预聚合统计

`DamageTracker` 维护五类预聚合条目：

| 条目 | 键 | 主要用途 |
|---|---|---|
| `outgoing` | 责任来源实例 | 某实例造成的伤害。 |
| `incoming` | 目标实例 | 某实例承受的伤害。 |
| `outgoingByType` | 责任来源类型 | 某类实体造成的伤害。 |
| `incomingByType` | 目标类型 | 某类实体承受的伤害。 |
| `global` | 无 | 世界全部有效伤害。 |

一次伤害会同时写入来源实例、目标实例、来源类型、目标类型和全局条目。冗余写入换取单来源、单目标和全局查询的精确快速读取。

实例级顶层条目默认最多 50 个，超过上限时按最久未交互淘汰；玩家条目不会被淘汰。按实体类型的条目永久保留。自定义名只缓存玩家名与被命名实体名，普通实体显示实体类型翻译。

### 5.3 聚合器与指标

`DamageAccumulator` 在顶层维护总量，同时拆分为三类子分组：

1. 按伤害类型。
2. 按直接来源实体类型。
3. 按对手。

会话的对手按具体实例分组，永久累计的对手按实体类型分组。分组子项不继续嵌套分组，避免维度组合指数增长。

指标包括最终伤害、原始伤害、格挡量、减免量、减伤率、命中数、击杀数、平均单次、最高单次、最低单次、时长、本场平均 DPS、实时 DPS 和每秒命中次数。

本场平均 DPS 的分母是首次命中至末次命中的真实时长；若都在同一游戏刻，最小分母为 1 tick。实时 DPS 是最近 `dpsWindowTicks` 内最终伤害之和除以窗口秒数，默认窗口为 100 tick，即 5 秒。

### 5.4 战斗会话

每个 `StatsEntry` 含有永久累计、可选的当前 `DamageSession` 和已结束会话摘要队列。

1. 首次有效伤害创建会话。
2. 每 20 tick 检查一次超时。
3. 超过 `sessionTimeoutTicks` 没有新伤害时，当前会话结束。
4. 已结束会话最多保留 `keepFinishedSessions` 个，默认 10 个。
5. 结束会话只保存总伤害、平均 DPS、时长和命中数。

当前会话不持久化，重启世界后会从下一次伤害开始新会话。

## 6. 查询与筛选

查询由四个可选槽位构成：来源、目标、直接来源和伤害类型。所有已填写条件使用逻辑与组合。

| 来源 | 目标 | 行为 |
|---|---|---|
| 有 | 有 | 只看来源对目标造成的伤害。 |
| 有 | 空 | 看来源对所有 `LivingEntity` 造成的伤害。 |
| 空 | 有 | 看目标承受的所有伤害。 |
| 空 | 空 | 世界全部伤害，仅管理员可查询。 |

实体条件支持两种粒度：

1. `EntitySelector.Instance`：指定 UUID 的具体实体。
2. `EntitySelector.Type`：指定实体类型下的所有实例。

伤害类型也支持两种粒度：

1. `DamageTypeSelector.Category`：由分类 JSON 定义的多个伤害类型集合。
2. `DamageTypeSelector.Exact`：单个注册表 ID。

只有“单来源”“单目标”和“全局”读取预聚合条目。来源与目标同时存在，或存在直接来源、伤害类型条件时，`StatsViewBuilder` 会扫描 `DamageLog` 后重新聚合。因此组合筛选只能覆盖仍在内存日志中的记录。

## 7. 伤害类型分类

分类文件是 `<配置目录>/damagestats-damage-types.json`。文件结构为：

```json
{
  "Magic": [
    "minecraft:magic",
    "minecraft:indirect_magic"
  ],
  "Physical": [
    "minecraft:player_attack",
    "minecraft:arrow"
  ]
}
```

分类名是英文标识。内置分类具有翻译，用户自定义分类没有翻译时直接显示原名；没有被任何分类包含的伤害类型显示完整注册表 ID。`/damagestats reload` 会重新读取该文件，之后生成的新快照使用新的分类结果。

GUI 的伤害类型列表可切换为“分类”或“注册表 ID”：前者合并同分类伤害，后者逐个列出所有已统计 ID，即使该 ID 已属于某分类也可精确筛选。

## 8. GUI 行为

按 `H` 打开统计 GUI。客户端先请求“我造成的伤害”快照，再显示界面。

GUI 包含以下控制：

| 控件 | 行为 |
|---|---|
| 造成 / 承受 | 决定当前主体进入来源槽或目标槽。 |
| 我 / 实例 / 类型 / 全部 | 循环选择主体；“全部”只对管理员开放。 |
| 本场 / 累计 | 在会话数据与永久累计数据之间切换。 |
| 伤害类型 / 直接来源 / 对手 | 选择当前分组列表。 |
| 分类 / 注册表 ID | 切换伤害类型分组模式。 |
| 详细指标 | 展开或折叠非核心指标。 |
| 历史战斗 | 查看已结束会话摘要。 |
| 清除筛选 | 清除四槽筛选并回到当前主体。 |
| 导出 | 将当前客户端快照写入客户端游戏目录。 |
| 清空我的 | 向服务端请求清除当前玩家的实例级条目。 |
| 浮层位置 | 打开 Overlay 位置与外观编辑界面。 |

分组行的点击行为如下：

1. 直接来源或伤害类型行直接加入对应筛选；再次点击取消。
2. 对手行会先询问“设为主体”还是“设为另一筛选项”。
3. 每个已有筛选在顶部有独立标签，点击标签可单独取消。
4. 服务端会重新授权筛选，并在快照中回传实际生效的筛选，客户端无条件采纳该结果。

当前 GUI 固定逻辑尺寸为 320×257。会话和累计数据都随快照传输，但当前实现以切换按钮显示其中一份，并非同屏展示。

## 9. Overlay 行为

按 `J` 开关 Overlay，默认关闭。Overlay 只展示当前玩家的责任来源输出，不受 `publicStats` 设置影响。

默认展示四项：

1. 目标范围名称，默认“全部目标”。
2. 总最终伤害。
3. 本场平均 DPS 与实时 DPS。
4. 命中次数。

若当前会话已结束，Overlay 显示永久累计并以灰色表现非活跃状态，实时 DPS 为零。GUI 的造成视图中选择目标实体类型后，客户端发送 `OverlayTargetTypePacket`，服务端仅为该玩家保存这项临时类型限制；退出服务器时清除，不持久化。

Overlay 使用客户端配置保存可见性、位置、缩放、背景透明度和四个字段开关。位置按屏幕比例保存并在渲染时夹住屏幕边界。位置编辑界面支持拖拽预览、完成或 Esc 保存、重置位置、缩放滑块、透明度滑块及字段复选框。

## 10. 网络协议

网络注册位于 `registry/DSPackets`，协议版本直接采用模组版本。六个 Payload 均为必需协议：

| 方向 | Payload | 作用 |
|---|---|---|
| 服务端到客户端 | `StatsSummaryPacket` | Overlay 字段增量。 |
| 服务端到客户端 | `StatsSnapshotPacket` | GUI 完整快照。 |
| 服务端到客户端 | `StatsInvalidatedPacket` | 重置后要求客户端丢弃旧快照。 |
| 客户端到服务端 | `StatsRequestPacket` | 请求带筛选、主体槽位和类型分组的 GUI 快照。 |
| 客户端到服务端 | `OverlayTargetTypePacket` | 设置或清除 Overlay 目标类型。 |
| 客户端到服务端 | `ResetStatsPacket` | 请求清空自己的实例级统计。 |

`StatsRequestPacket` 每位玩家最多每 2 tick 处理一次，超出的请求会直接丢弃。服务端根据权限修正筛选：非管理员请求全局统计或无权查看他人数据时，会降级为“我造成的伤害”。

Overlay 每 20 tick 为在线玩家计算摘要。`OverlaySummaryDelta` 用 6 位字段位图描述目标名、总伤害、平均 DPS、实时 DPS、命中次数和活跃状态；首包传全部字段，后续仅传与上一摘要不同的字段。

客户端 `ClientStats` 只保存最后一份 GUI 快照和 Overlay 摘要。离开世界、服务器重置后会清空对应内存状态。

## 11. 配置

### 11.1 服务端公共配置

| 键 | 默认值 | 范围或行为 |
|---|---:|---|
| `trackingEnabled` | `true` | 开关新伤害采集。 |
| `sessionTimeoutTicks` | `100` | 20 至 12000。 |
| `dpsWindowTicks` | `100` | 20 至 1200。 |
| `instanceEntryLimit` | `50` | 10 至 1000。 |
| `keepFinishedSessions` | `10` | 0 至 100。 |
| `autoSave` | `true` | 启动时读取、停服时写入统计。 |
| `autoSaveIntervalTicks` | `0` | 0 为关闭；最大 432000。 |
| `publicStats` | `true` | 是否允许普通玩家查看他人统计。 |
| `damageLogLimit` | `20000` | 1000 至 500000。 |

### 11.2 客户端配置

| 键 | 默认值 |
|---|---:|
| `overlayVisible` | `false` |
| `overlayX` | `0.01` |
| `overlayY` | `0.3` |
| `overlayScale` | `1.0` |
| `overlayBackgroundOpacity` | `0.5` |
| `overlayShowTarget`、`overlayShowDamage`、`overlayShowDps`、`overlayShowHits` | 均为 `true` |

## 12. 持久化、重置与导出

服务端统计文件为 `<世界目录>/damagestats/stats.json`。写入采用临时文件加原子移动；不支持原子移动的文件系统会回退为覆盖移动。读取失败时记录日志并从空统计开始，不阻止世界加载。

持久化包含实例统计、类型统计、名称缓存、全局统计、永久累计和已结束会话摘要。不包含正在进行的会话、DPS 窗口、原始日志和 Overlay 目标类型。

自动保存开启时，正常停服通过 `ServerStoppingEvent` 保存；`ServerStoppedEvent` 作为异常停服兜底。强制结束进程或断电只能依靠可选的定时保存降低数据丢失量。

重置语义：

1. 管理员命令 `/damagestats reset` 清空整个 `DamageTracker` 并通知所有客户端快照失效。
2. GUI “清空我的”仅移除该玩家作为来源或目标的实例条目；类型级和全局数据仍保留，因为其中混有其他实体的数据。
3. 自动保存关闭时，显式重置仍会读取并覆盖旧 `stats.json`，避免之后重新打开自动保存时旧数据复活。

导出使用同一套 `StatsExporter`：

| 入口 | 目录 | 权限 |
|---|---|---|
| GUI 导出 | `<游戏目录>/damagestats/exports/` | 不需要。 |
| `/damagestats export` | `<世界目录>/damagestats/exports/` | 权限等级 2，且命令执行者必须是玩家。 |

每次导出创建不重复的时间戳文件名。JSON 包含筛选、当前会话、永久累计、会话历史、三类分组与六项减免；CSV 仅写入当前会话和累计的分组行，适合表格软件分析。

## 13. 命令与按键

| 入口 | 行为 |
|---|---|
| `H` | 打开 GUI。 |
| `J` | 显示或隐藏 Overlay。 |
| `/damagestats`、`/damagestats out` | 输出自己的造成伤害。 |
| `/damagestats in` | 输出自己的承受伤害。 |
| `/damagestats history` | 输出自身已结束战斗。 |
| `/damagestats type <实体类型>` | 输出该类型的造成和承受累计；私有统计模式下仅管理员可用。 |
| `/damagestats toggle` | 切换采集开关，需要权限等级 2。 |
| `/damagestats reset` | 清空全局统计，需要权限等级 2。 |
| `/damagestats reload` | 重载伤害类型分类 JSON，需要权限等级 2。 |
| `/damagestats export` | 导出当前执行者自身的输出快照，需要权限等级 2。 |

## 14. 目录责任划分

| 目录或类 | 职责 |
|---|---|
| `handler/common/DamageCollectHandler` | 收集伤害事件。 |
| `stats/` | 原始记录、聚合、会话、日志和服务端统计入口。 |
| `stats/filter/` | 四槽筛选、主体槽位、类型分组和筛选键。 |
| `stats/view/` | 服务端视图构建、网络传输 DTO、Overlay 摘要与增量。 |
| `stats/save/` | 世界统计持久化和 JSON / CSV 导出。 |
| `registry/packet/` | C2S 与 S2C Payload。 |
| `client/screen/` | 统计 GUI 与 Overlay 位置编辑。 |
| `client/overlay/` | Overlay 渲染。 |
| `config/` | NeoForge 配置与分类 JSON 读取。 |
| `datagen/` | 中英文语言数据生成。 |
| `command/DSCommands` | 命令入口。 |

## 15. 已知限制与验收状态

当前构建已通过 `build runData`，中英文语言键集合一致，成品 JAR 不包含 datagen `.cache`。项目当前没有 `src/test` 或 `src/gameTest` 测试源，也未完成游戏内联机人工验收。

需要重点关注的限制：

1. GUI 使用固定 320×257 逻辑尺寸，长文本或低高度窗口可能溢出。
2. Overlay 固定 112 像素逻辑宽度，长目标名称不会自动裁剪或换行。
3. 活跃会话、名称缓存、请求限流记录和个人重置边界缺少完整的长期淘汰策略。
4. 大容量原始日志的组合筛选在服务端主线程全量扫描，且快照分组列表没有上限。
5. GUI 快照只在打开或变更筛选时刷新；GUI 导出的是客户端当前缓存快照，不是额外即时向服务端请求的结果。
6. 会话与永久累计都在快照中存在，但 GUI 当前只能切换查看，未同屏展示。

在补充筛选、DPS、重置持久化和网络编解码测试，并完成单人、专用服务端、多人与不同分辨率下的人工验收前，不应把该版本视为完整验收版本。
