# DamageStats 需求与验收基线

> 本文档描述当前有效需求、已确认取舍和与实现的差异。需求变更必须先修改本文档，再修改代码。
>
> 本文档中的“静态一致”表示已通过源码与构建配置复核；它不等同于完成单人、联机和不同界面尺寸下的游戏内验收。
>
> [焦点仪表盘需求.md](焦点仪表盘需求.md) 是 GUI、Overlay、同步、权限、多维查询与实例目录的覆盖性需求，并优先于本文档的旧设计描述。截至 2026-09-04，焦点协议、全屏 GUI、按需分页、完整事件日志、实例目录、Overlay 改造和 GUI 完整筛选结果导出均已实现；自动化测试不在当前范围内，游戏内人工验收尚未完成。

## 1. 项目范围

DamageStats 是面向 Minecraft 1.21.1 的 NeoForge 伤害统计模组。它在服务端采集 `LivingEntity` 受到的有效伤害，维护战斗会话和永久累计，并通过命令、GUI 和 Overlay 展示或导出统计结果。

| 项目 | 当前基线 |
|---|---|
| Minecraft | 1.21.1 |
| NeoForge | 21.1.233 |
| Java | 21 |
| 映射 | Parchment 2024.11.17 |
| modid | `damagestats` |
| 根包 | `io.zershyan.damagestats` |
| 部署模型 | 客户端和服务端均必须安装，协议版本必须一致 |
| 不在范围内 | KubeJS 接口、未安装本模组服务器上的客户端统计 |

## 2. 术语与伤害语义

| 术语 | 定义 |
|---|---|
| 责任来源 | 应归属这笔输出的实体。投射物的射手、驯服生物的主人会作为责任来源。 |
| 直接来源 | 实际接触目标的实体，例如箭、火球、狼或近战玩家本身；用于二次筛选。 |
| 目标 | 承受伤害的 `LivingEntity`。 |
| 环境 | 无实体来源的虚拟责任来源，覆盖摔落、岩浆、缺氧和 `/kill` 等。 |
| 原始伤害 | `LivingDamageEvent.Post#getOriginalDamage()`，即减免计算前的数值。 |
| 实际伤害 | `LivingDamageEvent.Post#getNewDamage()`，即减免后的最终伤害值；保留致死过量部分，不等同于目标生命值实际减少量。 |
| 战斗会话 | 同一统计对象连续收到有效伤害的一段时间；超过配置超时没有新伤害即结束。 |
| 永久累计 | 从当前世界统计开始持续累加的数据，只有显式重置才清零。 |

完全免疫、完全格挡或最终伤害不大于零的事件不计入命中、原始日志和聚合统计。

## 3. 已确认设计决策

| 编号 | 决策 |
|---|---|
| D1 | 服务端权威采集并同步给客户端；客户端不自行计算统计。 |
| D2 | 底层保留实体实例 UUID 与实体类型；GUI 可按实例或按类型查询。 |
| D3 | 会话与永久累计同时维护，且应在 GUI 中同屏展示；会话超时归档，累计仅靠显式重置清除。 |
| D4 | 无需预先配置规则即自动采集；Overlay 默认显示玩家对任意目标造成的伤害，可在 GUI 的造成视图中按目标类型限定。 |
| D5 | 提供本场平均 DPS 与最近 5 秒实时 DPS；同一游戏刻内的平均 DPS 最小分母为 1 tick。 |
| D6 | 同时记录原始伤害、最终伤害、格挡量和六项减免明细；默认展示最终伤害。 |
| D7 | 伤害类型分类由 JSON 映射决定，未分类类型显示注册表 ID，并可通过命令热重载。 |
| D8 | 统计归属责任来源，直接来源按实体类型聚合并作为独立筛选维度。 |
| D9 | 没有实体来源的伤害归入环境，不丢弃。 |
| D10 | 服务端数据按世界目录保存；客户端统计快照在断开连接时清除，Overlay 偏好和类型焦点按客户端配置保存，长期数据可导出。 |
| D11 | 实例候选目录只收录已记录实例；保留期采用现实时间，单位为小时，`instanceDirectoryRetentionHours` 默认 72；每个世界全局最多 `instanceDirectoryLimit` 个候选，默认 5000，超额时淘汰最久未交互者。按类型聚合数据与完整原始伤害历史不受候选目录淘汰影响。 |
| D12 | Overlay 默认隐藏，统计采集不受其可见性影响。 |
| D13 | 持久化完整原始伤害事件；来源、目标、直接来源和伤害类型的任意组合均可进行全历史精确查询。查询必须由索引、服务端排序和游标分页支撑，有限内存日志不得作为权威数据源。管理员主动清理全部记录后，以清理后的剩余事件作为新的完整历史。 |
| D14 | 当前不要求自动化测试；后续质量验证以构建、数据生成和游戏内人工验收为准。 |

## 4. 功能需求

### R1 采集开关

1. 模组加载后默认开始采集，不要求玩家先执行命令或修改配置。
2. `trackingEnabled` 配置项和 `/damagestats toggle` 都能开关采集。
3. 关闭采集后保留已有数据，重新开启后继续累加。
4. 关闭期间发生的伤害直接丢弃，不补记。

### R2 焦点、查询主体与权限

每位玩家拥有一个私有焦点，由来源和目标的类型或实例粒度构成。默认焦点为“当前玩家作为来源，目标不限”；页头与 Overlay 共用该焦点，但浏览其他条件不会自动改动焦点，必须明确点击“设为焦点”。

| 来源 | 目标 | 结果 |
|---|---|---|
| 指定 | 指定 | 指定来源对指定目标的伤害。 |
| 指定 | 空 | 指定来源对所有目标造成的伤害。 |
| 空 | 指定 | 指定目标承受的所有伤害。 |
| 空 | 空 | 世界全部伤害，仅权限等级 2 及以上可查询。 |

1. 普通玩家的页头来源固定为自身；只能选择目标类型或已记录目标实例，不能清空来源查看任意目标的全部承伤。
2. 普通玩家可以从自身责任来源的直接来源图表中下钻至具体 `LivingEntity` 实例，并仅将该合法实例替换为来源；服务端必须证明该实例来自该玩家自身的输出路径。
3. 权限等级 2 的管理员可以选择任意已记录 `LivingEntity` 类型或实例作为来源，并可查询全局组合；所有选择均须由服务端重新授权。
4. 当 `publicStats=false` 时，普通玩家查询必须涉及自身或其合法下钻实例；管理员不受此限。
5. 实体类型汇总和实体筛选统一通过 GUI 完成，不再提供切换查询目标的命令入口。

### R3 页间筛选、分组与分页

页间数据由来源、目标、直接来源和伤害类型组成的查询内核产生，其中只有来源、目标可以被设为焦点。直接来源和伤害类型是图表维度或临时筛选，不会隐式改动 Overlay。

| 维度 | 筛选粒度 | 分组与点击行为 |
|---|---|---|
| 来源 | 实例或实体类型 | `LivingEntity` 行可替换为来源，受 R2 权限约束。 |
| 目标 | 实例或实体类型 | `LivingEntity` 行可替换为目标。 |
| 直接来源 | 先按实体类型聚合，再选择具体已记录实例 | 非 `LivingEntity` 只能用于图表和筛选，不能设为来源或目标。 |
| 伤害类型 | JSON 分类或注册表 ID | 分类模式按分类聚合，注册表模式按 ID 精确筛选。 |

1. 页间按伤害总量降序绘制横向直方图；可选择伤害类型、直接来源，或在来源为空且目标非空时选择责任来源作为纵轴。
2. 图表和实例目录由服务端排序、搜索及游标分页，每页固定 50 行；客户端不得请求无上限的全量列表。
3. 类型聚合行不能直接代表某一实例；用户必须先进入该类型的已记录实例列表。
4. 完整原始事件持久化后，合法组合查询必须返回全历史精确结果，不得再以“有限日志已淘汰”为由降级为不完整数据。

### R4 指标与明细

默认界面展示总最终伤害、命中次数、本场平均 DPS、实时 DPS、平均单次伤害和最高单次伤害。详细指标展开后还应展示原始伤害、减免量与减伤率、统计时长、击杀数、最低单次伤害和每秒命中次数。

1. 伤害类型、直接来源、对手分组均按最终伤害降序，显示伤害、命中次数和占比。
2. 减伤率使用“原始伤害减最终伤害”的差值计算，而不是六项减免账目的简单和。
3. 六项减免明细为护甲、附魔、药水效果、伤害吸收、天生抗性和无敌帧；它们必须持久化并写入 JSON 导出。
4. 历史会话仅保留总伤害、平均 DPS、时长和命中次数摘要。

### R5 Overlay

1. Overlay 由独立可绑定按键开关，默认关闭，游戏中纯展示且不拦截输入。
2. Overlay 与 GUI 页头使用同一份服务端焦点摘要；默认对象是当前玩家对任意目标的输出。Overlay 设置只能选择目标，且该操作会修改唯一焦点。
3. 字段由客户端逐项勾选，至少包含来源与目标名称、原始与最终伤害、减免量和减伤率、原始与最终的平均 DPS / 实时 DPS、命中次数、平均单次伤害、最高贡献伤害类型、最高贡献直接来源、本场状态与时长。
4. 最高伤害的折前和折后为独立复选项，设置页必须有预览；两项同时勾选时显示 `最高伤害：x / x`，顺序固定为折前 / 折后。
5. 位置编辑只能从 GUI 的 Overlay 设置进入，支持拖拽、完成、Esc 保存和重置位置；坐标按屏幕比例保存，缩放范围为 50% 至 200%，背景透明度范围为 0% 至 100%。

### R6 持久化、重置与导出

1. 服务端必须在 `<世界目录>/damagestats/` 中持久化完整原始伤害事件及查询所需索引，不同世界天然隔离；旧的单个 `stats.json` 不得再作为唯一权威存储。
2. 持久化存储必须支持追加写入、索引重建、版本迁移、损坏恢复和有界批量落盘。具体文件布局、落盘频率与容量控制属于实施前必须完成的存储设计，不能沿用“停服才保存原始日志”的旧模型。
3. 进行中的会话可作为可重建聚合缓存，但完整原始事件、实例目录所需元数据及已结束会话摘要须按各自保留规则持久化。
4. 存储概览中的“清理全部记录”需要权限等级 2；固定页尾的“清空我的”仅清除当前玩家可清除的个人数据，具体影响范围必须在 UI 中明确展示并由服务端校验。
5. 显式重置必须原子地更新持久化存储和索引，不能留下旧事件可被重新加载。
6. 服务端命令导出到 `<世界目录>/damagestats/exports/`，需要权限；GUI 导出当前浏览筛选的完整结果到 `<游戏目录>/damagestats/exports/`，不需要额外权限，但服务端必须复核该筛选的浏览授权。
7. 每次导出生成同时间戳的 JSON 和 CSV。JSON 包含指标、筛选、本场与累计各三类分组、会话历史与减免明细；CSV 仅包含本场与累计全部分组明细。GUI 导出由服务端分块返回，单块最多 50 条分组或会话，客户端收齐后才写入本地磁盘。
8. 权限等级 2 的玩家可通过 `/damagestats storage` 打开服务器存储概览 GUI。概览至少显示事件数、分段数、原始段与压缩段占用、索引状态，以及缓存、服务器导出、临时文件和迁移备份占用。
9. 存储概览中的清理操作只能由管理员主动触发，且每项均须二次确认：清理临时文件/迁移备份不影响权威事件；清理服务器导出文件不影响统计；清理全部记录必须原子地清空事件日志、索引、聚合缓存和实例目录，并使后续精确查询基于清理后的新历史。客户端本地导出文件不属于服务器管理范围。

### R7 网络与客户端状态

1. GUI 页面快照、柱状图和实例候选只在打开界面、改变条件、切换维度、搜索、翻页或手动刷新时按需请求；服务端在授权后返回实际生效条件。
2. 有效伤害匹配玩家焦点时，服务端在同一 tick 内合并多条变化，并在 tick 结束前发送一份焦点摘要增量；首包和焦点切换后发送完整摘要。不得再按固定 20 tick 向所有在线玩家轮询同步。
3. 焦点摘要只更新 Overlay 和已打开 GUI 的固定页头，页间图表不得被后台推送自动刷新；非焦点伤害不向客户端发送统计数据。
4. 客户端断开世界时清除统计快照与实例焦点；类型焦点、Overlay 可见性、位置、外观和字段偏好按客户端配置保存。
5. 协议必须覆盖焦点完整摘要与增量、页面/图表请求、搜索与游标分页、实例目录、焦点设置、授权结果、Overlay 配置与重置操作；具体 Payload 列表随新协议实现确定。

### R8 入口

| 入口 | 行为 |
|---|---|
| `H` | 打开伤害统计 GUI。 |
| `J` | 开关 Overlay。 |
| `/damagestats toggle` | 切换伤害采集，需要权限等级 2。 |
| `/damagestats reload` | 重载伤害类型分类，需要权限等级 2。 |
| `/damagestats export` | 导出服务器统计，需要权限等级 2。 |
| `/damagestats storage` | 打开服务器存储概览，需要权限等级 2。 |

## 5. 配置默认值、迁移与边界

| 配置项 | 默认值 | 合法范围或说明 |
|---|---:|---|
| `trackingEnabled` | `true` | 总采集开关。 |
| `sessionTimeoutTicks` | `100` | 20 至 12000 tick。 |
| `dpsWindowTicks` | `100` | 20 至 1200 tick。 |
| `instanceDirectoryRetentionHours` | `72` | 单位现实小时；已记录实例目录的保留时长，不得影响完整原始事件。 |
| `instanceDirectoryLimit` | `5000` | 每个世界全局的已记录实例目录上限；超额时淘汰最久未交互者，不得影响完整原始事件。 |
| `keepFinishedSessions` | `10` | 0 至 100。 |
| `autoSave` | `true` | 是否按 `autoSaveIntervalTicks` 额外写出聚合缓存；完整事件和实例目录在正常停止时始终保存。 |
| `autoSaveIntervalTicks` | `0` | 定时写出聚合缓存的间隔；0 表示仅在正常停止、显式重置和需要时写出。 |
| `publicStats` | `true` | 关闭后普通玩家只能查询涉及自己的数据。 |
| `overlayVisible` | `false` | 客户端配置。 |
| `overlayX`、`overlayY` | `0.01`、`0.3` | 客户端比例坐标。 |
| `overlayScale` | `1.0` | 0.5 至 2.0。 |
| `overlayBackgroundOpacity` | `0.5` | 0 至 1。 |

伤害类型分类配置位于 `<配置目录>/damagestats-damage-types.json`，格式为“英文分类名到伤害类型 ID 列表”的 JSON 映射。未被分类的类型始终保留为其原始注册表 ID。

## 6. 复核结论

### 6.1 已确认实现事实

1. 伤害采集位于 `LivingDamageEvent.Post`，并丢弃非正最终伤害；完整事件以带校验和的二进制分段追加保存，段级索引可重建，旧 JSONL 可迁移。
2. 全屏统计页拥有固定页头、可滚动图表区和固定操作区；来源、目标、类型、实例、直接来源下钻、伤害分类/注册表 ID、责任来源和目标维度均由服务端分页返回。
3. Overlay 与页头共用服务端焦点。有效伤害仅在匹配当前焦点时触发同 tick 合并增量；焦点切换或登入时建立一次历史缓存，后续命中不回扫完整事件日志。
4. 页间图表、历史会话和服务端导出均在请求时从完整事件日志重建，因此关闭定时聚合快照后仍可查询已有历史。实例目录与名称缓存会随聚合缓存恢复，并按现实时间和数量上限清理。
5. 普通玩家来源固定为自身，只有从自身直接来源链路得到的具体 `LivingEntity` 实例可下钻为来源；管理员的来源/全局查询由服务端重新授权。
6. GUI 导出以受服务端授权的当前浏览筛选为范围，服务端分块传输完整快照，客户端仅在收齐全部块后写入本地 JSON 与 CSV。
7. 翻译由 `DSKeyLang` 与 `DSConfigLang` 声明并通过 datagen 生成，成品 JAR 排除了 datagen `.cache`。
8. 权限等级 2 的玩家可通过 `/damagestats storage` 打开全屏服务器存储概览；概览按需读取服务端目录，显示事件数、分段数、原始段、压缩段、索引、聚合缓存、服务器导出、临时文件、迁移备份和总占用，并展示当前索引恢复状态。三类清理均经独立确认页和服务端权限复核：临时文件/迁移备份、服务器导出、全部记录；全量清理通过存储概览复用统一的全局清理流程。

### 6.2 仍与需求或质量目标不一致的事项

| 编号 | 严重度 | 差异或风险 |
|---|---|---|
| F1 | 中 | 图表与实例目录已按页限制为 50 项，但完整事件查询和实例搜索运行在服务端主线程；极大历史数据仍需以游戏内压测验证响应时间。 |
| F2 | 中 | 不同分辨率、低高度窗口、超长自定义名和第三方注册表 ID 仅做了静态裁剪复核，尚未完成真实游戏 GUI/Overlay 验收。 |

### 6.3 已完成的静态验证

1. 使用项目已有 Java 21 与 Gradle 8.13 完成编译；不下载或替换 Gradle 发行版。
2. 翻译只通过 `DSKeyLang` 与 `DSConfigLang` 声明，并由 `runData` 生成中英文语言文件。
3. 图表与实例网络页面均有 50 条上限，焦点摘要使用完整包与增量包区分，并带单调修订号。
4. 当前不要求 `src/test` 或 `src/gameTest` 测试源；静态构建仍不能替代游戏内人工验收。

## 7. 后续验收门槛

在以下事项完成前，不应把项目标记为“需求全部验收通过”：

1. 在单人、专用服务端和多人权限场景进行游戏内验收，并验证高频伤害、大量实例、长会话、索引重建、尾帧损坏恢复、GUI 导出与 Overlay 布局。
2. 对长期高频伤害存档压测事件日志的磁盘增长、索引重建耗时、查询延迟和主线程占用。
