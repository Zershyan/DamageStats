# DamageStats 需求文档

> 本文档是实现工作的唯一依据。需求变更时，先改本文档，再改代码。
> 第 2 节的架构决策已由用户拍板，可直接实现。第 4 节的问题尚未定，动到相关代码前必须先问。

---

## 0. 项目定位与技术基线

DamageStats 是一个伤害统计模组。它记录游戏中发生的伤害事件，按玩家关心的维度聚合成统计结果，并通过 GUI 界面与屏幕常显浮层（Overlay）展示。

工程基线（读自现有配置，非需求）：

| 项 | 值 |
|---|---|
| Minecraft | 1.21.1 |
| NeoForge | 21.1.233 |
| Java | 21 |
| 映射 | Parchment 2024.11.17 |
| modid | `damagestats` |
| 根包 | `io.zershyan.damagestats` |
| 基础设施类前缀 | `DS`（依 `.claude/skills/neoforge-mod-style` 规范，取 modid 缩写） |
| Mixin 配置 | `damagestats.mixins.json`（已就位） |

---

## 1. 术语约定

后文一律使用这些词，避免"实体 A / 实体 B"这类会随上下文漂移的说法。

| 术语 | 含义 |
|---|---|
| **伤害事件** | 游戏内一次完整的伤害结算 |
| **责任来源** | 这次伤害最终该记在谁头上。玩家射的箭记玩家，玩家的狼咬人记玩家，苦力怕炸人记苦力怕。**这是统计的归属维度** |
| **直接来源** | 真正碰到目标的那个东西。箭、火球、狼本体、玩家的手。**这是界面上的二次筛选维度** |
| **目标** | 承受伤害的一方，一定是一个 `LivingEntity` |
| **伤害类型** | 原版 `minecraft:damage_type` 注册表中的条目，如 `minecraft:player_attack`、`minecraft:arrow` |
| **原始伤害** | 技能或武器理论上打出的数值，进入减免计算之前的值 |
| **实际伤害** | 扣掉护甲、抗性、附魔减免、伤害吸收之后，目标真正掉的血量 |
| **统计条目** | 一份聚合结果，内含总伤害、命中次数、按伤害类型/直接来源的分解、各项派生指标 |
| **战斗会话** | 一段连续交战的时间范围。超时无新伤害则自动结束，下次交战开一个新的 |
| **永久累计** | 从进入存档起一直往上加的总数据，只有玩家手动重置才清零 |
| **Overlay** | 游戏画面上常驻显示的信息浮层，不打开任何界面就能看到 |

---

## 2. 已确认的架构决策

以下 12 项已经用户拍板，是实现的直接依据。

| 编号 | 决策 | 内容 |
|---|---|---|
| **D1** | 服务端必装 | 服务端采集数据后通过网络包同步给客户端。换取数据完全精确、能拿到伤害类型和减免明细、能看到其他玩家的输出。放弃"在未装模组的服务器上使用"这一场景 |
| **D2** | 底层按实体实例记录 | 每条伤害记录都带实体的 UUID 和类型。界面上可以自由切换"看那只特定的 Boss"或"所有僵尸合并统计" |
| **D3** | 战斗会话 + 永久累计并存 | 两份数据同时维护、同时展示。战斗会话在超时无新伤害后自动结束；永久累计只有手动重置才清零 |
| **D4** | 全自动记录 + 准星锁定 | 不需要玩家提前配置任何规则，交过手就有数据。另外支持准星指着某个实体按键，把它锁定为 Overlay 的常显目标 |
| **D5** | DPS 算两种 | **本场平均 DPS** = 总伤害 ÷（首次命中到末次命中的时长），反映整场战斗的稳定输出。**实时 DPS** = 最近 5 秒滑动窗口，反映当前爆发。两个数字分开展示 |
| **D6** | 原始伤害和实际伤害都记 | 默认展示实际伤害，详情页里能看到"原始 100 / 被减免 35 / 实际 65 / 减伤率 35%" |
| **D7** | 伤害类型分类走配置文件映射，缺省回落到 ID | 配置文件按「分类 → 该分类下的伤害类型列表」组织，例如 `{"Magic": ["minecraft:magic", "minecraft:indirect_magic"]}`。分类名是**英文标识符**，显示时才翻译；玩家自己加的分类没有对应翻译就回落显示原名。**没被归类的伤害类型直接显示原始注册表 ID**，不会因为漏配而丢数据。改完可以用 `/damagestats reload` 热重载 |
| **D8** | 伤害归属到责任来源，界面按直接来源二次筛选 | 底层保留完整的来源信息（责任来源 + 直接来源）。召唤物和投射物的伤害计入主人的总伤，但界面上能筛出其中某个直接来源的部分。例：玩家用箭打 500、用剑打 150，总伤显示 650，旁边可以筛出"箭 500" |
| **D9** | 无实体来源的伤害归到"环境" | 摔落、岩浆、缺氧、`/kill` 这类伤害挂在一个虚拟的"环境"来源下，不丢弃 |
| **D10** | 服务端权威 + 客户端本地缓存 | 完整数据存服务端世界目录，跟着存档走。同时客户端把自己看得到的那部分缓存到本地，导出时玩家直接从自己的游戏目录拿文件，不需要服务器管理员配合 |
| **D11** | 实例条目限量，类型汇总永久保留 | 实例级条目只保留最近交战的若干个（默认 50），超出后按"最久没交互"淘汰。按实体类型的汇总数据体量小，永久保留。这样内存可控且不丢长期统计 |
| **D12** | Overlay 默认隐藏 | 装上模组后屏幕不会凭空多出东西。统计照常在后台跑，玩家按键自行打开浮层 |

### D8 的数据模型含义

D8 决定了每条伤害记录必须保存这些字段，缺一个都会导致某个维度无法筛选：

| 字段 | 用途 |
|---|---|
| 责任来源 UUID + 实体类型 | 归属聚合（"这笔算谁的输出"） |
| 直接来源 UUID + 实体类型 | 二次筛选（"其中箭打了多少"） |
| 目标 UUID + 实体类型 | 归属聚合（"这笔算谁受的伤"） |
| 伤害类型 ID | 类型明细（R3）与 D7 的分类映射 |
| 原始伤害、实际伤害 | D6 的双份统计 |
| 发生时刻 | DPS 滑动窗口（D5）、会话切分（D3）、最高单次伤害的时间戳 |

---

## 3. 功能需求

### R1 统计功能默认开启

> 原话：伤害统计默认为打开状态

- **R1.1** 模组装上就开始工作。玩家进入世界后无需执行指令、无需改配置、无需点任何开关，伤害数据就在后台采集。
- **R1.2** "默认开启"只约束**数据采集**这一件事。GUI 需要玩家主动打开，Overlay 默认隐藏（D12）。
- **R1.3** 提供总开关（配置项 + 运行时指令各一份）。关闭后停止采集新数据，已采集的数据不清空，重新打开后继续往上累加。
- **R1.4** 采集关闭期间发生的伤害**直接丢弃**，不做记录。`待确认 Q4`

**验收标准**：全新存档进入世界，直接用剑打一只僵尸，打开统计界面能看到这次伤害的数值。整个过程零配置。

---

### R2 统计维度

> 原话：可以以多种方式统计。最常见的为限定两个实体（玩家 A 对实体 B 的伤害）。可以单独限定一个实体（玩家 A 造成的伤害，不限目标 / 实体 B 受到的伤害，不限来源）。

一份统计由「来源条件 + 目标条件」两个槽位决定，每个槽位可以是「指定某个对象」或「任意」：

| 模式 | 来源槽 | 目标槽 | 语义 | 典型用途 |
|---|---|---|---|---|
| M1 定向统计 | 指定 | 指定 | 只统计 A 打 B 的伤害 | 看自己对 Boss 打了多少 |
| M2 输出统计 | 指定 | 任意 | A 造成的全部伤害 | 看自己一场战斗的总输出 |
| M3 承伤统计 | 任意 | 指定 | B 承受的全部伤害 | 看 Boss 一共吃了多少、团队总输出 |
| M4 全局统计 | 任意 | 任意 | 世界内所有伤害 | 调试用 |

- **R2.1** M1、M2、M3 必须支持。
- **R2.2** 因为采集是全自动的（D4），这三种模式不是"玩家提前设置的规则"，而是**查询与展示时的筛选方式**。底层只有一份完整的伤害记录，界面按不同维度聚合出不同视图。
- **R2.3** 槽位里"指定"的对象既可以是一个具体实体实例，也可以是一个实体类型（D2）。
- **R2.4** 界面上除了来源/目标两个槽位，还要有第三个筛选维度：**直接来源**（D8）。例：先筛出"我造成的全部伤害"，再筛"其中箭造成的部分"。
- **R2.5** 准星指着实体按键，把它锁定为 Overlay 的常显目标（D4）。
- **R2.6** M4 全局统计是否需要，尚未定。`待确认 Q3`

---

### R3 伤害类型明细

> 原话：默认不显示类型，玩家可以通过 UI 界面查看详细伤害类型。比如造成的 1000 伤害里，魔法伤害是多少，物理伤害是多少，可以直接写 id。

- **R3.1** 每条统计条目除总伤害外，额外维护「伤害类型 → 该类型累计伤害」的分组。
- **R3.2** 默认视图（Overlay 和 GUI 首页）**只显示总伤害**，不铺开类型分解。
- **R3.3** GUI 有详情视图，列出每种伤害类型的累计伤害、命中次数、占总伤害的百分比，按伤害量降序。
- **R3.4** 类型显示名走 D7：先查配置文件的分类归属，归了类就显示分类的译名（`Magic` → 「魔法伤害」），没归类则**直接显示注册表 ID**（如 `irons_spellbooks:fire_magic`）。第三方模组新增的伤害类型不会因为没配置而丢失。
- **R3.5** 分类映射配置支持热重载，`/damagestats reload` 即可，不必重启游戏。`已实现`

---

### R4 派生统计指标

> 原话：需要提供 dps、最高单次伤害等各种有用的数据统计，默认可以不显示，给玩家按钮自己点击查看。

- **R4.1** 默认视图只显示少量核心数据，其余指标折叠。GUI 里有按钮或标签页展开完整列表。
- **R4.2** 指标清单（P0 必做，P1 有余力再做）：

| 优先级 | 指标 | 算法 |
|---|---|---|
| P0 | 总伤害（实际） | 计入本条目的实际伤害之和 |
| P0 | 总伤害（原始） | 计入本条目的原始伤害之和（D6） |
| P0 | 被减免伤害 / 减伤率 | 原始总伤 − 实际总伤，以及其占原始总伤的比例 |
| P0 | 命中次数 | 计入本条目的伤害事件条数 |
| P0 | 平均单次伤害 | 总伤害 ÷ 命中次数 |
| P0 | 最高单次伤害 | 最大值，附带它的伤害类型、直接来源、发生时刻 |
| P0 | 本场平均 DPS | 总伤害 ÷（首次命中到末次命中的时长）（D5） |
| P0 | 实时 DPS | 最近 5 秒滑动窗口内的伤害 ÷ 5（D5） |
| P0 | 统计时长 | 本条目的时间跨度 |
| P1 | 最低单次伤害 | 最小值，排除 0 |
| P1 | 击杀数 | 本条目内导致目标死亡的次数 |
| P1 | 每秒命中次数 | 命中次数 ÷ 统计时长 |

- **R4.3** 战斗会话和永久累计各维护一套完整指标（D3）。DPS 对永久累计基本没有参考价值，界面上应当以会话数据为主。
- **R4.4** 滑动窗口 DPS 的窗口长度（默认 5 秒）应当可配置。
- **R4.5** NeoForge 的 `DamageContainer` 免费提供了减免的**六项分解**：护甲、护甲附魔、药水效果、伤害吸收、天生抗性、无敌帧。这六项已经在数据模型里逐条保留，GUI 详情页可以直接用，不需要额外计算。

---

### R5 Overlay 常显与自由拖拽定位

> 原话：ui 界面可以开启使用 overlay（layer）常显，常显时默认显示最有用的信息，然后这个显示位置可以通过设置按键点击后，拖拽到喜欢的位置，点击完成后，ui 固定在玩家自己拖拽到的位置。

- **R5.1** 通过 NeoForge 的 GUI Layer 机制注册屏幕浮层，不打开任何界面就能看到数据。可开可关，默认关闭（D12）。
- **R5.2** 正常游戏时浮层**完全不拦截鼠标和键盘**，纯展示。
- **R5.3** 默认显示四项：**目标名 + 总伤害 + DPS + 命中次数**。
- **R5.4** 位置编辑流程：
  1. 玩家按下绑定按键，进入「位置编辑模式」；
  2. 屏幕上出现浮层的可拖拽预览，带半透明背景框和边界高亮，让玩家看清占位范围；
  3. 按住鼠标左键拖到任意位置；
  4. 点击「完成」按钮或再按一次按键退出，坐标写入客户端配置，永久生效；
  5. 提供「重置为默认位置」入口，避免拖到屏幕外找不回来。
- **R5.5** 坐标以**相对屏幕的比例**保存，不用绝对像素。玩家改分辨率或切换窗口大小后浮层不会跑到屏幕外。
- **R5.6** 浮层显示的是准星锁定的目标（D4）；未锁定时显示最近交战的目标。
- **R5.7** 缩放比例、背景透明度、字段自选是否要做，尚未定。`待确认 Q6`

---

### R6 数据持久化与导出

> 原话：数据可以导出/保存，可以设置开启默认保存（退出游戏后持久化）等，但是保存的数据需要每个存档独立。

- **R6.1** **存档隔离是硬要求**。存档 A 的统计数据绝对不能出现在存档 B 里，也不能互相覆盖。服务端按世界目录隔离，客户端缓存按"服务器地址 + 世界标识"隔离。
- **R6.2** 提供「自动保存」配置项。开启时退出世界前自动写盘，下次进入**同一个存档**时自动恢复，接着上次继续累计。永久累计数据（D3）依赖这一项才有意义。
- **R6.3** 服务端权威数据存在世界目录下，客户端缓存存在客户端游戏目录下（D10）。
- **R6.4** 提供「手动导出」入口（GUI 按钮 + 指令各一份）。导出文件名带时间戳，不覆盖旧文件。
- **R6.5** 客户端玩家导出时读的是自己的本地缓存（D10），不需要服务器管理员配合。
- **R6.6** 导出格式尚未定。`待确认 Q7`
- **R6.7** 自动保存的默认值（开/关）尚未定。`待确认 Q8`
- **R6.8** 持久化用 NBT 还是独立 JSON 文件，尚未定。`待确认 Q9`

---

### R7 统计会话的生命周期

原始需求没提，但它决定"造成 5000 伤害"这句话的含义，已按 D3 定案。

- **R7.1** 每个统计对象同时维护**战斗会话**和**永久累计**两份数据，界面上同时展示。
- **R7.2** 战斗会话的结束条件：超过 N 秒（默认 5 秒）没有新的伤害计入，本次会话自动结束并归档。下一次伤害开启新会话。
- **R7.3** 超时时长可配置。
- **R7.4** 会话结束后的数据是否保留、保留几场，尚未定。`待确认 Q10`
- **R7.5** 提供手动重置永久累计的入口（指令 + GUI 按钮）。

---

### R8 单人 / 多人架构

已按 D1、D10 定案。

- **R8.1** 伤害采集全部在服务端进行。单人游戏走集成服务端，逻辑与多人一致，不写两套。
- **R8.2** 服务端通过网络包把统计数据同步给客户端。同步应当是**增量 + 节流**的，不能每次伤害都发全量包。
- **R8.3** 客户端只做展示和本地缓存，不自行计算权威数据。
- **R8.4** `neoforge.mods.toml` 中的依赖声明需要标明服务端必需。
- **R8.5** 采集点定在 `LivingDamageEvent.Post`。选它的原因是这个时机的数值已经定稿、不会再被别的 mod 改动，并且同时拿得到原始伤害、实际扣血、格挡量和六项减免明细。**代价**：最终伤害为 0 的攻击（完全免疫、完全格挡）不触发该事件，因此不计入统计——"打了但没造成伤害"不算一次命中。

---

### R9 多人环境下的数据可见性

- **R9.1** 玩家能否查看别人的伤害统计，尚未定。`待确认 Q11`
- **R9.2** 当前实现只让玩家查自己的数据：每秒推送和按需快照都只含请求者本人的统计。M3 想要的团队输出对比（某个 Boss 受到的伤害按来源细分）在服务端已经算好了，缺的只是客户端入口，等 Q11 定了再补。

---

### R10 界面入口与按键绑定

- **R10.1** 需要三个可绑定按键：打开统计 GUI、开关 Overlay、准星锁定目标。位置编辑模式的入口做在 GUI 里的「浮层位置」按钮上，没有单独占按键。
- **R10.2** 默认按键：GUI = `H`，Overlay 开关 = `J`，准星锁定 = `K`。`已实现`
- **R10.3** 所有按键都要能在原版按键设置界面里改，归到统一的 `DamageStats` 分类下。
- **R10.4** P1 已经提供 `/damagestats` 指令作为查询入口：`out` 看自己造成的伤害、`in` 看自己承受的、`reset` 清空统计、`reload` 重载分类映射配置。GUI 做出来之后指令仍然保留，R6.4 的导出子命令在 P5 补上。

---

## 4. 剩余待确认问题

这些都不阻塞 P1 数据内核，可以在进入对应阶段时再定。编号从 Q3 起，因为原 Q1、Q2 在确认过程中已经升级成了 D2 和 D4。

| 编号 | 关联 | 问题 | 建议方案 | 阻塞阶段 |
|---|---|---|---|---|
| Q3 | R2.6 | 需要 M4 全局统计吗？ | 需要，但默认关闭，只给调试用 | P3 |
| Q4 | R1.4 | 采集关闭期间的伤害丢弃还是记录？ | 直接丢弃 | P1 |
| Q5 | R3.5 | 分类映射配置要支持热重载吗？ | 提供重载指令 | `已实现` |
| Q6 | R5.7 | Overlay 要支持缩放、透明度、字段自选吗？ | 都要，但排在基础功能跑通之后 | P4 |
| Q7 | R6.6 | 导出格式选哪个？ | JSON + CSV 双份，JSON 给程序，CSV 给 Excel | P5 |
| Q8 | R6.7 | 自动保存默认开还是关？ | 默认开 | P5 |
| Q9 | R6.8 | 持久化用 NBT 还是独立 JSON 文件？ | 独立 JSON，可直接编辑和备份 | P5 |
| Q10 | R7.4 | 结束的战斗会话保留几场？ | 保留最近 10 场，可配置 | P1 |
| Q11 | R9.1 | 多人时能看别人的统计吗？ | 服务端配置决定，默认公开 | P2 |
| Q12 | R10.2 | 三个按键的默认绑定？ | GUI = `H`，Overlay = `J`，锁定目标 = `K` | `已实现` |
| Q13 | — | 需要 KubeJS 支持（把统计数据开放给整合包脚本）吗？ | 暂不做，等有需求再加 | — |

---

## 5. 建议实现顺序

每个阶段可以单独验收，后一阶段依赖前一阶段的产出。

| 阶段 | 内容 | 依赖决策 | 卡在哪些待确认项 |
|---|---|---|---|
| **P1 数据内核** `已完成` | 服务端伤害事件监听、伤害记录数据结构、责任来源/直接来源判定、按类型与来源分组、战斗会话切分、派生指标计算、实例条目淘汰 | D1 D2 D3 D5 D6 D8 D9 D11 | Q4 Q5 Q10 已按建议方案落地，并做成了配置项 |
| **P2 网络同步** `已完成` | 视图 DTO 层、四个网络包、服务端节流推送、客户端数据镜像 | D1 D10 | 只同步请求者自己的数据，看别人的统计等 Q11 |
| **P3 GUI 界面** `已完成` | 统计主界面、造成/承受与本场/累计切换、三个维度的分解列表 | D2 D4 D6 D7 D8 | Q3 的 M4 全局统计仍未做 |
| **P4 Overlay** `已完成` | 屏幕浮层渲染、位置编辑模式、拖拽定位、比例坐标持久化、准星锁定 | D4 D12 | Q12 已按建议落地；Q6 的缩放与字段自选仍未做 |
| **P5 持久化与导出** | 按存档隔离的读写、自动保存、手动导出 | D10 | Q7 Q8 Q9 |

P1 是一切的基础，且当前所有待确认项都有合理默认值，可以立即开工。

---

## 6. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-03 | 初版。整理用户口述的 6 条需求为 R1~R6，补充 R7~R10 四项隐含需求。经三轮确认，敲定 D1~D12 共 12 项架构决策，剩余 11 项非阻塞待确认 |
| 2026-09-03 | P1 数据内核完成。新增 R4.5（NeoForge 免费提供的六项减免明细）、R8.5（采集点选 `LivingDamageEvent.Post` 的理由与 0 伤害不计入的代价）、R10.4（`/damagestats` 指令）。Q4、Q5、Q10 按建议方案落地并做成配置项 |
| 2026-09-03 | 按用户纠正改两处：配置项显示名改走 `.translation()` 配合独立的 `DSConfigLang`（原先是中文 comment）；D7 的分类映射改成「分类 → 类型列表」、分类名用英文标识符、显示时才翻译（原先是「类型 → 中文分类名」）。同步给 skill 新增了 `references/config.md` |
| 2026-09-03 | 补齐 P1 的五处需求缺口（R1.3 开关指令、R3.3 命中次数、R7.4 会话历史入口、R2.1/R2.3 对手与类型汇总入口、R8.4 服务端必需声明），并把配置项常量改成 PascalCase 一并写进 skill |
| 2026-09-03 | P2、P3、P4 完成。视图 DTO 层的名称字段用 `Component` 传输，于是服务端英文、客户端中文时玩家看到的仍是中文；四个网络包按「每秒推 Overlay 摘要 + 打开界面时请求完整快照」分工，避免把大包塞进高频推送；Overlay 位置存屏幕比例并在拖拽时夹住边界，换分辨率不会跑偏。Q12 按建议方案落地 |

