# DamageStats 需求与验收基线

> **本文件是项目唯一有效的需求文档。需求变更必须先修改本文档，再修改代码。**
>
> **当前状态**（截至 2026-09-07）：
> - ✅ 核心功能已完成源码级实现：焦点协议、全屏 GUI、按需查询、完整事件存储、实例目录、Overlay、存储概览、GUI 导出
> - ✅ 已完成两轮设计缺陷复核与修复（并发安全、锁优化、实例保护、超时机制等）
> - ⏳ 待完成：游戏内人工验收与长期性能压测
> - ⛔ 不在范围：自动化测试、KubeJS 接口

## 1. 项目范围

DamageStats 是面向 Minecraft 1.21.1 的 NeoForge 伤害统计模组。服务端采集 LivingEntity 受到的有效伤害，维护战斗会话和永久累计，并通过 GUI、Overlay、命令和导出提供统计结果。

| 项目 | 约束 |
|---|---|
| Minecraft / NeoForge | 1.21.1 / 21.1.233 |
| Java / 映射 | Java 21 / Parchment 2024.11.17 |
| modid / 根包 | damagestats / io.zershyan.damagestats |
| 部署 | 客户端和服务端都必须安装，协议版本必须一致 |
| 不在范围内 | KubeJS 接口、未安装本模组服务器上的客户端统计 |

## 2. 伤害语义与统计范围

| 术语 | 定义 |
|---|---|
| 责任来源 | 应归属伤害的实体；投射物归属射手，驯服生物归属主人。 |
| 直接来源 | 实际接触目标的实体，例如箭、火球、狼或近战玩家。 |
| 目标 | 承受伤害的 LivingEntity。 |
| 环境 | 没有实体来源的虚拟来源，覆盖摔落、岩浆、缺氧和 /kill 等。 |
| 折前伤害 | LivingDamageEvent.Post#getOriginalDamage()，减免前数值。 |
| 折后伤害 | LivingDamageEvent.Post#getNewDamage()，减免后的最终数值；保留致死过量，不等同于生命值实际减少量。 |
| 战斗会话 | 同一统计对象连续收到有效伤害的一段时间；超过超时配置后结束。 |
| 永久累计 | 从当前世界开始统计后持续累加，只有显式重置或管理员清理才会清除。 |

最终伤害不大于零的事件不计入命中、原始事件日志或聚合统计。完整事件未被管理员清理时，任意合法来源、目标、直接来源和伤害类型组合都必须支持全历史精确查询；清理后以剩余记录为准。

本场与永久累计同时维护。DPS 包括本场平均 DPS 和最近窗口实时 DPS；同一游戏刻内计算平均 DPS 时，时间分母至少为 1 tick。默认界面显示折后总伤害、命中次数、本场平均 DPS、实时 DPS、平均单次伤害和最高单次伤害；展开明细后显示折前伤害、减免量与减伤率、统计时长、击杀数、最低单次伤害和每秒命中次数。

减免量使用“折前伤害 - 折后伤害”计算，减伤率使用该差值除以折前伤害，不能把各项减免账目简单相加。护甲、附魔、药水效果、伤害吸收、天生抗性和无敌帧六项明细必须持久化并写入 JSON 导出。已结束会话至少保存总伤害、平均 DPS、时长和命中次数摘要。

## 3. 采集开关、入口与配置

1. 模组加载后默认开启采集；关闭采集只保留已有数据，关闭期间产生的伤害直接丢弃，重新开启后继续累加。
2. trackingEnabled 配置项和 /damagestats toggle 都可切换采集，切换命令需要权限等级 2。
3. /damagestats reload 重载伤害类型分类，/damagestats export 导出服务器统计，二者需要权限等级 2。
4. H 打开伤害统计 GUI，J 开关 Overlay；/damagestats storage 由权限等级 2 的管理员打开存储概览。
5. 不提供切换目标、实体类型等复杂查询命令，相关操作统一放入 GUI。

| 配置项 | 默认值 | 说明 |
|---|---:|---|
| trackingEnabled | true | 总采集开关 |
| sessionTimeoutTicks | 100 | 合法范围 20～12000 tick |
| dpsWindowTicks | 100 | 合法范围 20～1200 tick |
| instanceDirectoryRetentionHours | 72 | 现实时间小时；实例目录保留期 |
| instanceDirectoryLimit | 5000 | 每世界全局实例候选上限 |
| keepFinishedSessions | 10 | 合法范围 0～100 |
| autoSave | true | 是否额外定期保存聚合缓存 |
| autoSaveIntervalTicks | 0 | 0 表示仅在正常停止、显式重置或需要时保存聚合缓存 |
| publicStats | true | 是否允许普通玩家查询公开统计 |
| overlayVisible | false | Overlay 客户端默认关闭 |
| overlayX / overlayY | 0.01 / 0.3 | 屏幕比例坐标 |
| overlayScale | 1.0 | 合法范围 0.5～2.0 |
| overlayBackgroundOpacity | 0.5 | 合法范围 0～1 |

伤害类型分类配置位于配置目录下的 damagestats-damage-types.json，格式为“英文分类名到伤害类型 ID 列表”的 JSON 映射。未分类类型保留注册表 ID；分类和注册表 ID 两种模式都可用于 GUI 筛选和图表。

## 4. 焦点、查询与权限

每位玩家拥有一个私有焦点。焦点只固定来源和目标，二者可以是实体类型或具体实例；直接来源和伤害类型是页间的临时筛选或分组维度，不会自动改变焦点。焦点版本在切换、实例粒度变化和重置时递增，客户端必须丢弃旧版本网络包。

默认焦点为“当前玩家作为来源、目标不限”。GUI 可以浏览其他允许查询的条件，但必须点击“设为焦点”后才更新 Overlay 和页头焦点。GUI 页头与 Overlay 使用同一份服务端焦点摘要。

| 来源 | 目标 | 查询结果 |
|---|---|---|
| 指定 | 指定 | 指定来源对指定目标造成的伤害 |
| 指定 | 不限 | 指定来源对所有目标造成的伤害 |
| 不限 | 指定 | 指定目标承受的所有伤害，并可按责任来源拆分 |
| 不限 | 不限 | 世界全部伤害，仅权限等级 2 及以上可查询 |

权限规则：

1. 普通玩家的来源固定为自己，不能从页头来源选择器改成其他玩家或实体类型；可以选择目标类型或已记录目标实例。
2. 普通玩家在自己的直接来源图表中下钻到某个具体 LivingEntity 实例后，可以把该实例设为来源，例如把自己的狼设为来源。服务端必须证明该实例来自玩家自身的伤害路径。
3. 下钻例外不能通过目标承伤视图、无关事件或类型聚合绕过授权；切换目标或关闭 GUI 后回收该例外并恢复自身来源。
4. 权限等级 2 的管理员可以从页头选择任意已记录的 LivingEntity 类型或具体实例，也可以清空来源查询全局组合。
5. publicStats=false 时，普通玩家的查询必须涉及自己或合法下钻得到的实例；所有权限均由服务端重新校验，客户端提交的条件不具有授权效力。
6. 实体类型筛选列出注册表中的全部 LivingEntity 类型；实例筛选只列出已有统计记录的实例，不扫描当前加载实体。

## 5. 全屏 GUI

GUI 覆盖整个可用游戏区域，分为固定页头、唯一可滚动的页间和固定页尾：

- 页头显示当前浏览的来源、目标、类型或实例、焦点状态和摘要；摘要信息比 Overlay 更多，并同时显示本场与永久累计。
- 页头的来源、目标选择都打开独立窗口。目标窗口支持按显示名或注册表 ID 搜索全部实体类型，并支持进入已记录实例列表。
- 玩家实例按名称区分；其他实例显示自定义名、最近交互伤害、命中、最后交互时间、维度和坐标等信息。
- 页头提供“设为焦点”“刷新”“Overlay 设置”“导出”等日常操作。正在浏览的条件不同于焦点时，必须明确显示差异。
- 页间显示本场和累计摘要、维度选择、横向直方图、详细拆分、当前行和数据完整性说明；页间数据只按需请求，不因后台焦点更新自动刷新。
- 页尾固定，仅放“清空我的”、管理员重置等低频破坏性操作，并要求二次确认；来源、目标、维度和刷新等日常操作不放在页尾。
- 不同分辨率和低高度窗口下，摘要卡片自动换行或分页，文本裁剪并提供完整提示，不得重叠或越界。

### 5.1 图表、筛选与实例下钻

页间图表按当前浏览条件生成，横向条形按折后伤害从高到低排序；Y 轴为当前维度，X 轴为伤害值。每行显示精确折前、折后伤害、占比和命中次数，提示信息还应包含减免、直接来源和可用实例信息。

可选维度：

| 维度 | 规则 |
|---|---|
| 伤害类型（分类） | 按 JSON 分类合并并排序 |
| 伤害类型（注册表 ID） | 按单个注册表 ID 精确筛选 |
| 直接来源 | 先按实体类型聚合，再进入该类型的已记录实例列表 |
| 责任来源 | 仅来源为空且目标已指定时可选 |
| 目标 | 仅来源已指定且目标为空时可选 |

来源和目标都已指定时，责任来源和目标维度不再作为拆分维度。图表和实例列表由服务端搜索、排序和游标分页，每页最多 50 行，客户端不得请求无上限列表。类型聚合行不能直接冒充一个实例。

图表中代表 LivingEntity 的行可选择“设为来源”“设为目标”或“查看实例”；前两项替换对应条件并保留其他条件，随后重新按需请求。箭、火球、TNT 等非 LivingEntity 直接来源只能作为图表或筛选维度，不能设为来源或目标。

示例：玩家 P 的狼 W 攻击坚守者 S，P 又用剑攻击 S。查询 P→S 并按直接来源分组时显示 P 和 W 的两行；点击 W 后选择“设为来源”，查询替换为具体 W 实例→S。若图表行是“狼”类型聚合，必须先选择已记录的狼实例，再确定 W。

## 6. Overlay

Overlay 默认关闭，由独立按键开关，只负责展示，不拦截输入，也不承担查询和权限逻辑。它与 GUI 页头使用相同的服务端焦点摘要；默认显示当前玩家对任意目标造成的伤害。

Overlay 设置从 GUI 页头进入，目标选择复用 GUI 的类型搜索和实例选择，但不提供来源选择：

- 来源固定为当前玩家；
- 目标可以是任意目标、某个 LivingEntity 类型或具体已记录实例；
- 修改 Overlay 目标即修改共用焦点，页头和 Overlay 一起切换；
- 可配置显示内容，并提供勾选后的实时预览。

至少支持以下独立字段：来源与目标名称、折前和折后总伤害、被减免伤害与减伤率、折前和折后本场平均 DPS、折前和折后实时 DPS、命中次数、平均单次伤害、最高贡献伤害类型及数据、最高贡献直接来源及数据、本场状态和时长。

“最高伤害显示”必须分开勾选：

    最高伤害显示： [ ] 折前    [ ] 折后

只勾选一项时仅显示该值；两项同时勾选时单行显示“最高伤害：x / x”，顺序固定为折前 / 折后。字段名称必须明确统计范围和折前、折后语义。

Overlay 位置、缩放、背景透明度、可见性和字段偏好保存到客户端配置；位置支持拖拽、完成、Esc 保存和重置，坐标使用屏幕比例，缩放为 50%～200%，背景透明度为 0%～100%。类型焦点按服务器地址和世界标识保存，实例焦点断开连接即清除。

## 7. 持久化、查询、重置与存储概览

服务端数据按世界保存于世界目录下的 damagestats，世界之间天然隔离。完整原始事件采用不依赖 SQLite 的“二进制分段文件 + 段级索引”：

| 文件 | 作用 |
|---|---|
| events/segment-序号.bin | 最多 512 条事件的追加式分段；每帧含长度和 CRC32，尾部半帧或校验失败帧可安全忽略 |
| events/index.bin | 来源、目标、直接来源和伤害类型的段级索引；原子替换，缺失或损坏时从段文件重建 |
| events/resets.bin | “清空我的”的实体可见性边界，不删除共享事件 |
| events/segment-*.jsonl.migrated | 旧 JSONL 导入后的迁移副本，便于核对 |
| exports/ | 服务端导出文件 |

要求：

1. 事件追加写入、索引重建、版本迁移、损坏恢复和有界批量落盘必须可用；活跃段定期批量落盘，正常停服时封存并更新索引，异常中止后启动扫描恢复。
2. 段索引只能缩小扫描范围，最终结果必须逐条通过统一筛选器校验，以保证 JSON 分类热重载后仍正确。
3. 封存段可压缩以降低磁盘占用；完整事件、实例目录元数据和已结束会话摘要按各自规则保存。
4. 实例目录保存已记录实例的 UUID、类型、名称或自定义名、最近交互时间、维度、坐标和用于区分实例的统计信息；按现实时间 72 小时默认保留，每世界最多 5000 个，超额淘汰最久未交互者。目录淘汰不能删除完整原始事件。
5. 管理员通过 /damagestats storage 打开存储概览。概览显示事件数、分段数、原始和压缩段占用、索引状态、聚合缓存、服务端导出、临时文件、迁移备份和总占用，并显示索引恢复状态。
6. 存储清理只能由管理员在 GUI 中主动触发，并逐项二次确认：清理临时文件和迁移备份、清理服务端导出、清理全部记录。前两项不影响统计；全部记录必须原子清空事件日志、索引、聚合缓存和实例目录，之后精确查询只基于新历史。客户端本地导出文件不属于服务器清理范围。
7. “清空我的”只清除当前玩家有权清除的个人可见数据，范围须在界面明确并由服务端校验。
8. 服务端命令导出到世界目录下的 damagestats/exports，GUI 导出当前浏览筛选的完整结果到客户端游戏目录下的 damagestats/exports。两者均生成同时间戳的 JSON 和 CSV；GUI 导出由服务端分块传输，每块最多 50 条分组或会话，客户端收齐后再异步写盘。
9. JSON 包含筛选、指标、本场和累计三类分组、会话历史和减免明细；CSV 包含本场和累计全部分组明细。

## 8. 网络同步与客户端状态

1. 首次打开 GUI、改变来源、目标、实例、统计范围或图表维度、搜索、翻页、手动刷新、设为焦点时，服务端在授权后按需返回相应快照及实际生效条件。
2. 有效伤害完成后，服务端只判断受影响的在线焦点；同一服务器 tick 内合并同一焦点的多条命中，在 tick 结束前发送一份字段增量。首包和焦点切换后发送完整摘要。
3. 不允许每秒向所有在线玩家轮询计算 Overlay。非焦点伤害不发送统计数据；焦点伤害只更新 Overlay 和已打开 GUI 的固定页头，不推送页间图表。
4. 应使用来源、目标等订阅索引缩小候选，再进行完整筛选；匹配伤害不能每次回扫完整日志，焦点切换时允许按需建立一次缓存。
5. 客户端断开世界时清除统计快照和实例焦点；类型焦点、Overlay 显示状态、位置、外观和字段偏好按客户端配置保存。
6. 协议必须覆盖焦点完整和增量摘要、页面与图表请求、搜索分页、实例目录、焦点设置、授权结果、Overlay 配置和重置操作；摘要使用单调版本号拒绝乱序包。

## 9. 复核状态与验收门槛

### 9.1 源码级复核确认

以下功能已在代码层面实现完毕：

1. ✅ LivingDamageEvent.Post 采集有效伤害，完整事件使用带校验和的二进制分段保存，支持段级索引重建、旧 JSONL 迁移、尾帧损坏降级和封存段压缩
2. ✅ StatsFilter 支持来源、目标、直接来源和伤害类型的统一组合筛选；服务端图表和实例列表支持搜索、排序和每页 50 条分页
3. ✅ FocusSummary 和 FocusSummaryDelta 支持完整摘要与字段增量；同 tick 合并推送，焦点切换和登录时建立历史缓存
4. ✅ 普通玩家来源授权、直接来源实例下钻、管理员全局查询、实例目录保留和淘汰、断线清理均已接入服务端校验
5. ✅ 全屏固定页头、滚动页间、固定页尾 GUI，来源和目标弹窗，实例下钻，Overlay 字段双选预览，位置配置，存储概览和三类主动清理入口已实现
6. ✅ GUI 导出支持服务端授权、分块传输和客户端收齐后生成 JSON 与 CSV；翻译通过 DSKeyLang、DSConfigLang 和 datagen 生成，不能手改生成的语言 JSON
7. ✅ 并发安全优化：ServerStats 使用 volatile，DamageEventJournal 使用 ReadWriteLock 改善查询并发性能
8. ✅ 实例目录淘汰保护：玩家和有自定义名称的实体（驯服生物、命名怪物）永不被淘汰
9. ✅ 焦点会话超时改为每 tick 检查，确保状态瞬间同步
10. ✅ 导出分块传输添加 10 秒超时保护，防止网络丢包导致功能卡死
11. ✅ 下钻授权在目标改变时重新验证，防止权限绕过
12. ✅ 实体名称长度限制降至 64 字符，防止 GUI 溢出和网络带宽浪费

### 9.2 仍需完成的验收

以下范围需要游戏内人工验收和压测：

- 单人、专用服务端、多人权限场景的游戏内人工验收；
- 不同分辨率、低高度窗口、超长名称和第三方伤害类型 ID 的 GUI、Overlay 布局验收；
- 高频伤害、长期存档、实例上限、索引重建、尾帧损坏恢复、导出和清理流程的性能与恢复压测。

当前已知风险：

- 完整事件查询和实例搜索运行在服务端主线程，极大历史数据的响应时间需压测；
- 实时数据变化时，整数偏移分页可能出现重复或跳过，应以游标一致性压测验证；
- 直接来源实例筛选边界仍需在游戏内验证。

自动化测试不在当前需求范围内，但构建、数据生成和游戏内人工验收不能省略。

## 10. 开发约束

1. 使用项目已有的 Gradle 8.13 和缓存，不下载或替换新的 Gradle。
2. 翻译键只通过项目现有的语言数据生成流程声明和生成；不得手改生成的语言 JSON，翻译变更后使用项目 Gradle 执行 runData。
3. 新增 Java 注释必须使用简体中文；除非确有必要，不增加空构造方法。
