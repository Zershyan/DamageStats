# DamageStats 需求与验收基线

> 本文档描述当前有效需求、已确认取舍和与实现的差异。需求变更必须先修改本文档，再修改代码。
>
> 本文档中的“静态一致”表示已通过源码与构建配置复核；它不等同于完成单人、联机和不同界面尺寸下的游戏内验收。

## 1. 项目范围

DamageStats 是面向 Minecraft 1.21.1 的 NeoForge 伤害统计模组。它在服务端采集 `LivingEntity` 受到的有效伤害，维护战斗会话和永久累计，并通过命令、GUI 和 Overlay 展示或导出统计结果。

| 项目 | 当前基线 |
|---|---|
| Minecraft | 1.21.1 |
| NeoForge | 21.1.233 |
| Java | 21 |
| 映射 | Parchment 2024.11.17 |
| modid | `damagestats` |
| 根包 | `io.zershyan.damagestats` |
| 部署模型 | 客户端和服务端均必须安装，协议版本必须一致 |
| 不在范围内 | KubeJS 接口、未安装本模组服务器上的客户端统计 |

## 2. 术语与伤害语义

| 术语 | 定义 |
|---|---|
| 责任来源 | 应归属这笔输出的实体。投射物的射手、驯服生物的主人会作为责任来源。 |
| 直接来源 | 实际接触目标的实体，例如箭、火球、狼或近战玩家本身；用于二次筛选。 |
| 目标 | 承受伤害的 `LivingEntity`。 |
| 环境 | 无实体来源的虚拟责任来源，覆盖摔落、岩浆、缺氧和 `/kill` 等。 |
| 原始伤害 | `LivingDamageEvent.Post#getOriginalDamage()`，即减免计算前的数值。 |
| 实际伤害 | `LivingDamageEvent.Post#getNewDamage()`，即减免后的最终伤害值；保留致死过量部分，不等同于目标生命值实际减少量。 |
| 战斗会话 | 同一统计对象连续收到有效伤害的一段时间；超过配置超时没有新伤害即结束。 |
| 永久累计 | 从当前世界统计开始持续累加的数据，只有显式重置才清零。 |

完全免疫、完全格挡或最终伤害不大于零的事件不计入命中、原始日志和聚合统计。

## 3. 已确认设计决策

| 编号 | 决策 |
|---|---|
| D1 | 服务端权威采集并同步给客户端；客户端不自行计算统计。 |
| D2 | 底层保留实体实例 UUID 与实体类型；GUI 可按实例或按类型查询。 |
| D3 | 会话与永久累计同时维护，且应在 GUI 中同屏展示；会话超时归档，累计仅靠显式重置清除。 |
| D4 | 无需预先配置规则即自动采集；Overlay 默认显示玩家对任意目标造成的伤害，可在 GUI 的造成视图中按目标类型限定。 |
| D5 | 提供本场平均 DPS 与最近 5 秒实时 DPS；同一游戏刻内的平均 DPS 最小分母为 1 tick。 |
| D6 | 同时记录原始伤害、最终伤害、格挡量和六项减免明细；默认展示最终伤害。 |
| D7 | 伤害类型分类由 JSON 映射决定，未分类类型显示注册表 ID，并可通过命令热重载。 |
| D8 | 统计归属责任来源，直接来源按实体类型聚合并作为独立筛选维度。 |
| D9 | 没有实体来源的伤害归入环境，不丢弃。 |
| D10 | 服务端数据按世界目录保存；客户端只保留当前内存快照，长期留档通过导出完成。 |
| D11 | 实例级顶层条目默认最多 50 个，按最久未交互淘汰；按类型汇总永久保留。永久累计的对手分组按类型聚合，会话内按实例聚合。 |
| D12 | Overlay 默认隐藏，统计采集不受其可见性影响。 |
| D13 | 含组合筛选的查询扫描有限长原始日志；仅单来源、单目标和全局基础视图读取预聚合数据。 |

## 4. 功能需求

### R1 采集开关

1. 模组加载后默认开始采集，不要求玩家先执行命令或修改配置。
2. `trackingEnabled` 配置项和 `/damagestats toggle` 都能开关采集。
3. 关闭采集后保留已有数据，重新开启后继续累加。
4. 关闭期间发生的伤害直接丢弃，不补记。

### R2 查询主体与权限

来源、目标可分别为空、具体实例或实体类型，组合语义如下：

| 来源 | 目标 | 结果 |
|---|---|---|
| 指定 | 指定 | 指定来源对指定目标的伤害。 |
| 指定 | 空 | 指定来源对所有目标造成的伤害。 |
| 空 | 指定 | 指定目标承受的所有伤害。 |
| 空 | 空 | 世界全部伤害，仅权限等级 2 及以上可查询。 |

1. GUI 的“造成 / 承受”决定主体写入来源槽还是目标槽。
2. GUI 的“我 / 实例 / 类型 / 全部”决定主体对象；类型和实例行可选择“设为主体”或“设为另一筛选项”。
3. 当 `publicStats=false` 时，普通玩家只能查询至少有一个来源或目标槽为自己的数据；管理员不受此限。
4. `/damagestats type <实体类型>` 保留实体类型汇总入口。

### R3 四槽筛选与分组

每个查询有四个独立筛选槽，空值表示不限：

| 槽位 | 筛选粒度 | 分组与点击行为 |
|---|---|---|
| 来源 | 实例或实体类型 | 对手行可填入来源槽。 |
| 目标 | 实例或实体类型 | 对手行可填入目标槽。 |
| 直接来源 | 实体类型 | 直接来源列表按类型合并；再次点击取消该筛选。 |
| 伤害类型 | JSON 分类或注册表 ID | 分类模式按分类合并，注册表模式逐 ID 精确筛选；再次点击取消。 |

1. 顶部显示每项已生效筛选，点击单个标签可取消对应条件。
2. “清除筛选”回到当前主体的基础视图。
3. 本场对手按实例列出，累计对手按实体类型列出。
4. 带来源和目标双槽、直接来源或伤害类型的查询依赖原始日志；日志淘汰后必须标记“数据不完整”。

### R4 指标与明细

默认界面展示总最终伤害、命中次数、本场平均 DPS、实时 DPS、平均单次伤害和最高单次伤害。详细指标展开后还应展示原始伤害、减免量与减伤率、统计时长、击杀数、最低单次伤害和每秒命中次数。

1. 伤害类型、直接来源、对手分组均按最终伤害降序，显示伤害、命中次数和占比。
2. 减伤率使用“原始伤害减最终伤害”的差值计算，而不是六项减免账目的简单和。
3. 六项减免明细为护甲、附魔、药水效果、伤害吸收、天生抗性和无敌帧；它们必须持久化并写入 JSON 导出。
4. 历史会话仅保留总伤害、平均 DPS、时长和命中次数摘要。

### R5 Overlay

1. Overlay 由独立可绑定按键开关，默认关闭，游戏中纯展示且不拦截输入。
2. 默认字段为目标名、总伤害、平均 DPS / 实时 DPS 和命中次数；四个字段可分别关闭。
3. 默认对象是当前玩家对任意目标的输出；在 GUI 的造成视图中选择目标实体类型后，改为该类型目标的玩家输出。
4. 位置编辑只能从 GUI 的“浮层位置”进入，支持拖拽、完成、Esc 保存和重置位置。
5. 坐标按屏幕比例保存；缩放范围为 50% 至 200%，背景透明度范围为 0% 至 100%。

### R6 持久化、重置与导出

1. 服务端持久化文件为 `<世界目录>/damagestats/stats.json`，不同世界天然隔离。
2. `autoSave=true` 时在停服时写盘，启动同一世界时恢复；可选定时保存默认关闭。
3. 进行中的会话和原始筛选日志不持久化；已结束会话仅持久化摘要。
4. `/damagestats reset` 清空全局数据且需要权限等级 2；GUI “清空我的”只移除当前玩家的实例级来源和目标条目，不改类型汇总和全局汇总。
5. 显式重置不受自动保存开关影响，必须覆盖旧持久化文件。
6. 服务端命令导出到 `<世界目录>/damagestats/exports/`，需要权限；GUI 导出到 `<游戏目录>/damagestats/exports/`，不需要权限。
7. 每次导出生成同时间戳的 JSON 和 CSV。JSON 包含指标、筛选、三类分组、会话历史与减免明细；CSV 仅包含分组明细。

### R7 网络与客户端状态

1. GUI 快照仅在打开 GUI 或变更筛选时按需请求；服务端在授权后返回实际生效的筛选条件。
2. Overlay 每 20 tick 最多同步一次，首包完整，后续采用字段位图仅同步变化字段。
3. 客户端只保存内存快照，退出世界时清空。
4. 当前协议有 6 个必需 Payload：GUI 快照、Overlay 摘要、快照失效通知、GUI 请求、Overlay 目标类型和个人重置。

### R8 入口

| 入口 | 行为 |
|---|---|
| `H` | 打开伤害统计 GUI。 |
| `J` | 开关 Overlay。 |
| `/damagestats`、`out`、`in` | 查看自身造成或承受的统计。 |
| `/damagestats history` | 查看自身已结束会话。 |
| `/damagestats type <实体类型>` | 查看该实体类型的造成和承受累计。 |
| `/damagestats toggle`、`reset`、`reload`、`export` | 需要权限等级 2。 |

## 5. 配置默认值与边界

| 配置项 | 默认值 | 合法范围或说明 |
|---|---:|---|
| `trackingEnabled` | `true` | 总采集开关。 |
| `sessionTimeoutTicks` | `100` | 20 至 12000 tick。 |
| `dpsWindowTicks` | `100` | 20 至 1200 tick。 |
| `instanceEntryLimit` | `50` | 10 至 1000。玩家实例不参与淘汰。 |
| `keepFinishedSessions` | `10` | 0 至 100。 |
| `autoSave` | `true` | 关闭时启动不读取旧统计。 |
| `autoSaveIntervalTicks` | `0` | 0 表示不定时保存，最大 432000 tick。 |
| `publicStats` | `true` | 关闭后普通玩家只能查询涉及自己的数据。 |
| `damageLogLimit` | `20000` | 1000 至 500000 条；日志只在内存中存在。 |
| `overlayVisible` | `false` | 客户端配置。 |
| `overlayX`、`overlayY` | `0.01`、`0.3` | 客户端比例坐标。 |
| `overlayScale` | `1.0` | 0.5 至 2.0。 |
| `overlayBackgroundOpacity` | `0.5` | 0 至 1。 |

伤害类型分类配置位于 `<配置目录>/damagestats-damage-types.json`，格式为“英文分类名到伤害类型 ID 列表”的 JSON 映射。未被分类的类型始终保留为其原始注册表 ID。

## 6. 复核结论

### 6.1 静态一致项

R1、R2、R3、R4、R5、R6、R7 的采集、筛选、网络、持久化和入口链路均已有相应实现。已确认以下事实：

1. 伤害采集位于 `LivingDamageEvent.Post`，并丢弃非正最终伤害。
2. 双主体、直接来源、分类筛选和注册表 ID 精确筛选均有端到端数据结构与 GUI 入口。
3. Overlay 默认是当前玩家输出，且目标类型限制通过独立服务端状态实现。
4. 重置在自动保存关闭时仍会改写旧 `stats.json`。
5. 翻译由 `DSKeyLang` 与 `DSConfigLang` 声明并通过 datagen 生成，成品 JAR 排除了 datagen `.cache`。

### 6.2 仍与需求或质量目标不一致的事项

| 编号 | 严重度 | 差异或风险 |
|---|---|---|
| G1 | 中 | D3 与“会话和永久累计同时展示”的表述不一致：`StatsScreen` 通过“本场 / 累计”按钮二选一绘制。快照同时携带两份数据，但界面未同屏展示。 |
| G2 | 中 | 统计面板固定为 320×257，列表、筛选标签和最高单次文本没有裁剪、换行或悬浮完整文本；低高度窗口、长实体名和第三方注册表 ID 可能越界或互相覆盖。 |
| G3 | 中 | 活跃会话按对手实例保存聚合器与 DPS 队列，DPS 队列只在读取时清理；名称缓存、请求限流记录和个人重置边界也没有完整淘汰策略。持续作战或大量不同玩家进入的长期开服会增加内存。 |
| G4 | 中 | 原始日志允许最高 500000 条，带筛选查询在服务端主线程全量扫描并构建结果；本场实例分组和快照分组列表没有传输上限，极端情况下会形成大包和卡顿。 |
| G5 | 低 | GUI 请求采用 2 tick 直接丢包限流，连续快速操作可能暂时保留旧快照；个人重置包没有频率限制，可能重复触发同步写盘。 |
| G6 | 低 | Overlay 使用固定 112 像素逻辑宽度且不裁剪目标文本，长自定义名或第三方实体名可能溢出背景和屏幕。 |
| G7 | 高 | 当前没有单元测试、GameTest 或联机人工验收。编译通过不能证明伤害数值、权限、筛选交互和 GUI 布局正确。 |

### 6.3 已完成的静态验证

1. 已使用现有 Java 21 与 Gradle 8.13 成功执行 `build runData`。
2. `runData` 重复执行未产生语言文件变更；中英文语言文件均为有效 JSON，键集合一致，均为 104 项。
3. 成品 `build/libs/damagestats-0.0.1-alpha.jar` 不包含 `src/generated/resources/.cache` 内容。
4. 当前源码共有 58 个 Java 文件；项目没有 `src/test` 或 `src/gameTest` 测试源。

## 7. 后续验收门槛

在以下事项完成前，不应把项目标记为“需求全部验收通过”：

1. 决定并实现 G1 的同屏展示，或正式修改该需求为“可切换展示”。
2. 对低高度和长文本场景进行 GUI / Overlay 人工验收，并处理 G2、G6。
3. 为筛选、DPS 边界、重置持久化、网络编解码和权限增加自动化测试。
4. 在单人、专用服务端和多人权限场景进行游戏内验收。
5. 为大日志、长会话和高频重置设定容量与频率控制策略。
