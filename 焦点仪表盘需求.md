# DamageStats 焦点仪表盘需求

> 状态：焦点协议、全屏 GUI、Overlay、实例目录、完整事件分段存储、组合查询和 GUI 完整筛选结果导出已经实现；自动化测试不在当前范围内，真实游戏验收仍未完成。
>
> 本文档以 2026-09-04 确认的焦点仪表盘交互为准，描述目标行为、数据边界和实现约束。所有已知架构决策均已确认；其结论已同步至 `REQUIREMENTS.md`。

## 1. 改造目标

当前小尺寸筛选 GUI、每秒一次的 Overlay 推送和“分组行直接切换筛选”的交互应停止作为主方案。新方案引入每个玩家独立的**焦点**：

1. 焦点由来源、目标及其类型或实例粒度定义。
2. Overlay 与 GUI 页头展示同一份焦点摘要；GUI 页头展示的字段比 Overlay 更多。
3. 发生会影响当前焦点的有效伤害时，服务端立即向该玩家同步焦点摘要。
4. 非焦点数据不做定时同步；详细柱状图、实例候选和其他页面数据只在打开页面、改变条件、切换维度或手动刷新时按需请求。
5. GUI 改为全屏仪表盘，使用清晰的来源、目标、焦点、图表和设置入口，不再把复杂筛选塞进 320×257 面板。

“立即”描述的是网络触发时机：有效伤害记录完成后，在**同一服务器 tick 结束前**发起焦点更新，不再等待固定 20 tick 轮询。同一 tick 内的多条匹配伤害合并为一份最新摘要增量，延迟最多 1 tick。

### 1.1 已确认的事件存储方案

完整原始事件采用“二进制分段文件 + 段级索引”，不引入 SQLite 或其他额外运行时依赖。文件位于每个世界的 `damagestats/events/` 目录：

| 文件 | 职责 |
|---|---|
| `segment-<序号>.bin` | 固定最多 512 条事件的追加式二进制段；每条帧包含长度和 CRC32，尾部半帧或校验失败帧会在读取时被忽略。 |
| `index.bin` | 已封存段的来源、目标、直接来源及伤害类型段级索引；原子替换写入，损坏或丢失时从段文件重建。 |
| `resets.bin` | 单实体“清空我的”可见性边界；不删除共享事件，只让该实体的实例筛选忽略边界前的数据。 |
| `segment-*.jsonl.migrated` | 旧 JSONL 段首次导入后保留的迁移副本，便于管理员核对。 |

活跃段每 100 tick 批量落盘，正常停服时封存并更新索引；异常中止后的未封段在下次启动时扫描恢复。段索引只能减少不可能匹配的磁盘扫描，最终结果必须逐条使用 `StatsFilter` 匹配，保证 JSON 分类热重载后仍然正确。原始事件的长期保留与容量上限尚待确认，不能把“永久保存”误解成不受控制地无限占用磁盘。

## 2. 焦点模型

### 2.1 焦点是什么

焦点是当前玩家主动设置的一份私有查询状态，包含：

| 字段 | 含义 |
|---|---|
| 来源条件 | 普通玩家必须为自身或合法下钻得到的实例；管理员可为空、实体类型或具体实体实例。 |
| 目标条件 | 可为空、实体类型或具体实体实例。 |
| 统计范围 | 页头同时展示本场与累计；Overlay 的本场与累计字段独立勾选。 |
| 焦点版本 | 每次切换来源、目标、实例或重置时递增，用于丢弃乱序网络包。 |

焦点不把直接来源和伤害类型作为固定条件。它们是页间柱状图的拆分维度与按需筛选项。若之后需要“只把弓箭伤害设为焦点”，应作为独立扩展，而不是混入本次来源/目标焦点定义。

### 2.2 焦点与当前查询的关系

GUI 可以浏览任意允许查询的来源、目标和图表维度，但**只有点击“设为焦点”后**，当前来源与目标组合才会驱动页头和 Overlay。这样浏览其他对象不会让 Overlay 在无意间跳变。

Overlay 设置界面的目标选择与 GUI 焦点共用唯一状态：Overlay 设置目标即修改全局焦点目标，页头和 Overlay 在收到新的焦点完整摘要后同时变化。后一次“设为焦点”或 Overlay 目标选择覆盖前一次焦点目标，这是唯一明确的覆盖规则。

### 2.3 焦点默认值与清除

默认焦点为“当前玩家作为来源，目标不限”。清除目标后回到该状态；普通玩家不能清除来源，管理员可以。类型焦点按服务器地址和世界标识持久化到客户端配置；实例焦点在断线时清除，不跨连接保存。客户端持久化的仅是焦点选择，不是服务端统计数据。

## 3. 同步与按需加载

### 3.1 焦点即时同步

服务端完成一条有效 `DamageRecord` 后，判断其是否匹配在线玩家的焦点来源与目标。匹配时：

1. 更新该玩家的焦点会话与累计摘要。
2. 在本服务器 tick 内合并同一焦点的多次匹配记录，在 tick 结束前发送一份字段位图增量包；首包和焦点切换后发送完整焦点摘要。
3. 客户端将摘要同时提供给 Overlay 与已打开 GUI 的固定页头。

焦点摘要包应包含完整可配置字段所需的原始数据，而不是只发送已显示文本。客户端才根据本地 Overlay 配置决定展示哪些字段。

### 3.2 禁止后台定时同步

删除“每秒为所有在线玩家计算并发送 Overlay 摘要”的模型。以下操作才允许发送详细数据：

| 触发 | 服务端响应 |
|---|---|
| 首次打开 GUI | 返回当前焦点完整摘要和默认页间柱状图。 |
| 改变来源、目标、实例、统计范围或图表维度 | 返回对应的完整按需页面快照。 |
| 点击手动刷新 | 返回当前页间数据。 |
| 点击设为焦点 | 返回新焦点完整摘要，并开始即时焦点同步。 |
| 有效伤害匹配焦点 | 仅推送焦点摘要增量，不推送柱状图。 |
| 非焦点伤害 | 不向客户端发送任何统计数据。 |

页间柱状图不因焦点伤害自动刷新。用户必须通过手动刷新，或改变条件后重新请求，以换取低网络流量；即时推送只更新 Overlay 与固定页头。

### 3.3 服务器侧性能要求

不能在每次伤害发生时遍历所有在线玩家的完整焦点并全量重算。实现应建立来源和目标的索引，例如按“当前玩家自身来源”“实体类型目标”“具体目标 UUID”索引订阅者，再以完整过滤做最后校验。

焦点切换时可以按需计算一次完整摘要；之后匹配伤害应以增量方式更新该焦点，不允许每次伤害回扫原始日志。

## 4. 全屏 GUI 结构

GUI 应覆盖整个可用游戏界面，使用稳定的三段布局：

```text
+------------------------------------------------------------------+
| 固定页头：来源 -> 目标、实例选择、焦点摘要、操作区               |
+------------------------------------------------------------------+
|                                                                  |
| 可滚动页间：维度切换、柱状图、详细拆分、实例候选                |
|                                                                  |
+------------------------------------------------------------------+
| 固定页尾：可选；仅放不适合页头的少量破坏性操作                  |
+------------------------------------------------------------------+
```

### 4.1 页头

页头固定在顶部，至少分为两行：

1. **条件行**：来源、目标、类型 / 实例标识、切换和清除操作。
2. **焦点摘要行**：与 Overlay 同源的关键指标卡片，另增加 GUI 专属指标卡片。

页头应有明确的“正在浏览”和“当前焦点”状态。浏览条件不同于焦点时，显示“设为焦点”按钮；二者相同时按钮禁用或标记为已设焦点。

建议把以下非破坏性操作放入页头右侧：

| 操作 | 说明 |
|---|---|
| 设为焦点 | 将当前来源与目标应用为 Overlay / 页头焦点。 |
| 刷新 | 重新按需请求页间图表。 |
| Overlay 设置 | 打开 Overlay 专属配置界面。 |
| 导出 | 导出当前浏览筛选下的完整结果。服务端复核授权后分块返回本场与累计的全部三类分组及会话历史，客户端收齐后写入本地 JSON 与 CSV。 |

“清空我的”与管理员全局重置属于破坏性操作。可放入页尾或页头的更多菜单，并要求二次确认；最终位置见 Q7。

### 4.2 页间

页间是唯一滚动区域。固定页头和页尾不随滚轮移动；滚轮、滚动条、触控板滚动都只影响页间。

页间从上到下至少包含：

1. 本场与累计的并排摘要；页间图表可切换自己要查看的本场或累计范围。
2. 图表维度选择器。
3. 横向直方图。
4. 当前行、数据完整性和实例粒度说明。
5. 可选的完整指标和减免明细。

小屏幕下，页头摘要卡片必须自动换行或横向分页；图表标签和数值必须裁剪并提供悬浮完整文本，不能重叠或超出屏幕。

### 4.3 页尾

页尾固定存在，只放“清空我的”“管理员重置”等低频、破坏性操作，并要求二次确认。不能把来源、目标、维度、刷新等日常操作放在页尾。

## 5. 来源与目标选择

### 5.1 来源选择

来源选择必须打开独立弹窗，不能用循环按钮。弹窗内容受权限限制：

1. 普通玩家默认锁定为自己，不能通过页头任意选择其他来源。
2. 管理员可以选择任意已记录的 `LivingEntity` 类型和具体实例。
3. 当前来源为玩家类型时，实例选择弹窗应按玩家名列出可选玩家；没有实例选择时，表示该玩家类型的聚合。
4. 由页间直接来源条目下钻得到的实体实例是普通玩家的唯一额外来源入口，规则见第 6 节。

### 5.2 目标选择

目标选择必须打开独立弹窗，分为类型选择和实例选择两步：

1. 类型选择列出注册表中所有 `LivingEntity` 类型，支持按显示名和注册表 ID 搜索。
2. 类型选择后，可以保持类型聚合，也可以打开实例选择。
3. 实例选择只显示当前统计数据可识别的该类型实例；每项展示可区分信息，例如自定义名或玩家名、最近交互伤害、最后交互游戏刻和最后记录位置。
4. 选择实例后，目标过滤到该 UUID；选择“全部该类型”后，目标过滤到该实体类型。

`DamageRecord` 已记录维度 ID、方块坐标、游戏刻和现实时间；实例目录额外维护显示名与最近交互信息，因此实体死亡、卸载或重启后仍可用于已记录实例选择。

### 5.3 来源与目标的可空性

焦点默认使用“来源为当前玩家，目标不限”。目标可以清除为不限。普通玩家不能清除来源；管理员可清除来源，并在填写目标后查看目标承受的全部伤害及其责任来源拆分。

## 6. 页间横向直方图

### 6.1 通用规则

直方图以横向条形图表达伤害，而不是仅显示数字：

1. Y 轴是当前维度的名称，X 轴是伤害值。
2. 条形按最终伤害从高到低排序，最大条占可用宽度；每行同时显示精确数值、占比和命中次数。
3. Tooltip 显示完整名称、原始伤害、最终伤害、减免、命中、占比和可用的实例信息。
4. 图表需要虚拟滚动或分页，不能把全部行一次性构造成网络快照和渲染对象。
5. 统计来源是“当前浏览条件”，不是强制等于当前焦点；点击“设为焦点”才使两者一致。

### 6.2 可选维度

| 维度 | 可用条件 | Y 轴含义 |
|---|---|---|
| 伤害类型：分类 | 始终可选 | JSON 分类合并后的伤害类型。 |
| 伤害类型：注册表 ID | 始终可选 | 单个 `minecraft:damage_type` ID。 |
| 直接来源 | 始终可选 | 先按实体类型聚合；点击类型行后再选择该类型的已记录实例。 |
| 责任来源 | 来源为空且目标不为空 | 对当前目标造成伤害的责任来源实体。 |
| 目标 | 目标为空且来源不为空 | 当前来源造成伤害的目标实体。 |

当来源与目标都已填写时，不显示“责任来源”和“目标”维度，因为它们已被固定；仍可显示伤害类型和直接来源。

### 6.3 行点击与替换语义

页间中代表 `LivingEntity` 的任意行可点击，并弹出以下动作：

| 操作 | 结果 |
|---|---|
| 设为来源 | **替换**当前来源条件为该条目的类型或实例；目标和其他图表条件保留。 |
| 设为目标 | **替换**当前目标条件为该条目的类型或实例；来源和其他图表条件保留。 |
| 查看实例 | 若该行是类型聚合，打开该类型下的已记录实例列表。 |
| 取消 | 不改变页面。 |

非 `LivingEntity` 直接来源，例如箭、火球或 TNT，只能用于直接来源图表 / 筛选，不能被设为来源或目标。它们的责任来源行仍可以是玩家、狼等 `LivingEntity`。

### 6.4 狼与坚守者示例

设定：玩家 P 的狼 W 攻击坚守者 S，玩家 P 又用剑攻击 S。

1. 页头选择来源 P、目标 S，维度选择“直接来源”。
2. 图表应显示 P 的近战直接来源与 W 的直接来源两行，各自以伤害为横条。
3. 点击 W 后选择“设为来源”，当前来源被替换为 W 的**具体实例**，目标仍为 S。
4. 页间按当前条件重新请求，显示 W 对 S 的伤害分解；此时普通玩家允许该来源实例，因为它来自其自身来源视图中的直接来源下钻。
5. 如果同类型有多只狼，点击“狼”类型行后先打开已记录狼实例列表，再选择具体 W；类型聚合行绝不直接冒充某一个实例。

## 7. 实例数据与可用范围

实例不是永久存在的游戏对象。死亡、卸载、重启后都无法通过世界对象直接查到。因此实例选择应基于“已有统计记录的实体索引”，而不是扫描世界中所有当前加载实体。

建议每个可选择实例至少保存：

| 信息 | 用途 |
|---|---|
| UUID、实体类型 | 精确筛选。 |
| 玩家名或自定义名 | 人类可读识别。 |
| 最近交互游戏刻 | 排序和区分。 |
| 最近交互维度与坐标 | 区分同类型 Boss 或怪物。 |
| 与当前查询的伤害、命中 | 在实例选择界面中识别对象。 |

实例候选只来自已记录实例，不扫描所有当前加载实体。每个世界的实例目录按现实时间保留最近交互过的候选，保留时长由 `instanceDirectoryRetentionHours` 配置，默认 72 小时；全局最多保留 5000 个，数量上限由 `instanceDirectoryLimit` 配置。超出数量上限时按最久未交互顺序淘汰。目录保留必要的名称、最后交互位置和区分数据，淘汰不影响 Q12 所定义的完整原始伤害历史。

## 8. Overlay 设置界面

Overlay 设置从 GUI 页头的“Overlay 设置”进入，独立于位置编辑页面。该界面包含两组内容。

### 8.1 焦点目标

1. Overlay 不允许单独选来源，来源默认当前玩家。
2. 目标选择复用 GUI 的类型搜索和实例选择界面。
3. 可以选择“任意目标”、某个实体类型或具体实例。
4. 按第 2 节的默认解释，改变该目标会改变焦点，页头和 Overlay 同时切换。

### 8.2 显示与外观

现有位置、缩放、背景透明度、可见性和字段勾选应保留。字段选择扩展为至少支持：

| 字段 | 数据含义 |
|---|---|
| 来源与目标名称 | 当前焦点范围。 |
| 最终伤害总量 | 减免后的总伤害。 |
| 原始伤害总量 | 减免前总伤害。 |
| 被减免伤害与减伤率 | 原始伤害减最终伤害及比例。 |
| 减免前平均 DPS / 实时 DPS | 使用原始伤害计算。 |
| 减免后平均 DPS / 实时 DPS | 使用最终伤害计算。 |
| 命中次数与平均单次伤害 | 焦点聚合指标。 |
| 最高单次原始伤害 | 独立字段，按原始伤害排序。 |
| 最高单次最终伤害 | 独立字段，按最终伤害排序。 |
| 最高贡献伤害类型 | 伤害类型名称、最终伤害和占比。 |
| 最高贡献直接来源 | 直接来源名称、最终伤害和占比。 |
| 本场状态与时长 | 会话是否活跃、本场持续时间。 |

Overlay 只负责字段展示，不应承担来源权限、数据查询或实例管理逻辑。焦点筛选与即时同步由服务端统一处理，Overlay 配置仅为客户端显示偏好。

“最高伤害显示”必须使用两个独立复选框，并在设置界面提供实时预览：

```text
最高伤害显示： [ ] 折前    [ ] 折后
```

仅勾选一项时只显示对应数值；两项同时勾选时，Overlay 使用单行 `最高伤害：x / x`，顺序固定为“折前 / 折后”。减免前、减免后平均 DPS 与实时 DPS同样是独立可选字段，不允许把两个语义不明的值合并成一个配置项。

## 9. 权限模型

### 9.1 普通玩家

普通玩家：

1. 页头来源固定为当前玩家自身，不能从来源选择器改成其他玩家或其他实体类型。
2. 可以选择任意允许查看的目标类型和已记录目标实例。
3. 在“自身来源”的直接来源图表中，点击其自身伤害链条里的 `LivingEntity` 直接来源后，可以将**该具体实例**替换为来源。
4. 这项例外只允许从当前玩家自己的输出路径取得；不能借此跳到其他玩家、无关怪物或任意实体类型。
5. 仍受 `publicStats` 规则约束：当服务端关闭公开统计时，查询必须涉及当前玩家或该玩家从自身伤害路径取得的允许实例。

### 9.2 管理员

权限等级 2 的管理员可以查询全局统计，并可从页头来源选择器中选择任意已记录的 `LivingEntity` 类型和具体实例。无论管理员选择什么来源或目标，都应在服务端重新校验，而不能相信客户端发送的选择。

### 9.3 服务端授权原则

客户端只提交“想选择什么”；服务端决定“实际生效什么”。拒绝或降级时，返回明确的授权结果和可本地化的提示，而不是静默变成其他筛选，避免用户不知道自己在看谁的数据。

## 10. 当前实现边界

| 模块 | 当前职责 |
|---|---|
| `StatsSyncHandler` | 通过来源、目标和直接来源订阅索引锁定候选玩家；同 tick 合并命中，并仅推送发生变化的焦点摘要。 |
| `FocusSummaryCache` | 玩家登入或焦点切换时从完整历史建立一次缓存，之后只增量接收命中的伤害；会话超时只更新受影响的焦点。 |
| `FocusSummary`、`FocusSummaryDelta` | 页头与 Overlay 共用的完整/增量摘要契约，带单调修订号以拒绝乱序包。 |
| `FocusChartPage`、`EntityChoicePage` | 服务器排序、搜索和游标分页，每页最多 50 项；客户端不接收无上限列表。 |
| `StatsFilter` | 来源、目标、直接来源、伤害类型的统一查询内核；焦点只固定来源和目标。 |
| `DamageEventJournal` | 完整原始事件的分段保存、索引重建、旧 JSONL 迁移、尾帧损坏降级和个人重置可见性边界。 |
| `DamageRecord`、`InstanceDirectory` | 记录事件位置、现实时间与实体元数据；实例候选按保留期和数量上限维护。 |
| `StatsOverlay`、`OverlayPositionScreen` | 根据客户端字段配置裁剪焦点摘要，支持作用域、最高伤害双选预览、比例位置、缩放和背景透明度。 |
| `StatsScreen` 与选择弹窗 | 全屏固定页头/操作区、可滚动图表区、来源目标选择、实例下钻、权限提示和重置确认。 |
| `ExportRequestPacket`、`ExportStartPacket`、`ExportChunkPacket`、`ClientExportManager` | 服务端复核当前浏览筛选的授权，构建完整导出快照并按最多 50 条分块传输；客户端按请求号和分块序号重组，收齐后异步写入本地导出目录。 |
| 存储概览 GUI | 管理员通过 `/damagestats storage` 查看服务器事件数、分段、各类磁盘占用与索引状态；只能主动二次确认清理临时文件/迁移备份、服务器导出或全部记录。清理全部记录后，完整历史以剩余事件为准。 |

## 11. 需求漏洞与设计风险

### 11.1 焦点同步与网络风暴

已采用服务器 tick 为合并边界：同一 tick 内多条匹配伤害只发送最终摘要增量，摘要包只编码变化字段；来源、目标和直接来源索引先缩小订阅候选，再以完整筛选复核。高人数服务器仍需要实际压测索引命中率与网络占用。

### 11.2 来源选择权限边界

已确定管理员可选择任意已记录 `LivingEntity` 类型和实例，普通玩家的页头来源固定为自身。普通玩家的直接来源实例例外必须由服务端证明其来自当前玩家自身输出路径，且不得从类型聚合直接绕过实例选择。

### 11.3 类型聚合无法直接变成实例

已确定直接来源先按类型聚合，点击类型后必须进入实例选择列表。类型行只能设为类型来源或打开实例列表，不能直接声称代表某一实例。

### 11.4 任意组合的永久精确统计成本很高

已确定持久化完整原始事件，保证所有来源、目标、直接来源和伤害类型组合的全历史精确查询。该决定要求采用可追加、可索引、可迁移且可容错的持久化事件存储，不能继续把有限内存 `DamageLog` 作为权威数据；存档体积、索引重建、写入批处理和损坏恢复必须纳入实现计划与游戏内验收。

### 11.5 “所有游戏内生物”与实例候选不是同一件事

实体类型从注册表完整列出；实例候选只来自已记录实例，并受到现实时间与数量双重限制。`instanceDirectoryRetentionHours` 默认 72 小时，`instanceDirectoryLimit` 默认每个世界全局 5000 个，超额时淘汰最久未交互者。完整原始事件永久保存不代表实例选择列表无限增长。

### 11.6 同一焦点的范围语义

页头固定并排展示本场与累计。Overlay 对本场、累计、原始、最终和最高伤害字段逐项勾选；字段名称必须带上范围与伤害语义，不允许使用无范围的“总伤害”或无语义的“DPS”。

### 11.7 权限例外可被滥用

普通玩家允许从自身直接来源下钻到狼实例是合理需求，但必须防止它借由“查看某个目标承伤”或第三方事件得到不属于自身的实体来源。服务端要保留“该实例由当前玩家自身责任来源造成的伤害路径中出现”的证明，且例外是否在退出 GUI 或切换目标后保留也需明确。

### 11.8 图表数据没有分页会重现大包问题

已确定服务器排序、服务器搜索、游标分页且每页固定 50 行。客户端只能请求下一页或新搜索，不能要求服务端一次发送全部实例或全部图表行。

### 11.9 焦点与 Overlay 设置覆盖

已确定 GUI 与 Overlay 共用唯一焦点，后一次焦点修改覆盖前一次。GUI 必须显示当前焦点版本与浏览条件是否不同，避免用户误以为浏览某个目标已经修改了 Overlay。

## 12. 已确认决策与剩余参数

以下架构决策已经确认，正文必须以本表为准。

| 编号 | 已确认结论 |
|---|---|
| Q1 | 同一服务器 tick 内的多条匹配伤害合并，在 tick 结束前发送一份最新焦点摘要增量。 |
| Q2 | 只即时更新固定页头与 Overlay；页间柱状图仅在手动刷新或条件变化后按需加载。 |
| Q3 | 管理员可选择任意已记录的 `LivingEntity` 类型和具体实例作为来源。 |
| Q4 | 页头同时展示本场与累计；Overlay 的字段可分别选择本场或累计语义。 |
| Q5 | GUI 与 Overlay 共用唯一焦点；Overlay 改目标即修改该焦点。 |
| Q6 | 类型焦点按服务器地址和世界标识保存在客户端；实例焦点断线即清除。 |
| Q7 | 破坏性操作置于固定页尾。 |
| Q8 | 普通玩家不得清空来源查看任意目标的全部承伤；来源只能是自身或从自身输出路径合法下钻的实例。 |
| Q9 | 直接来源先按实体类型聚合；点击后再选择该类型下的具体已记录实例。 |
| Q10 | 实例候选仅来自已记录实例，且必须同时受保留时长和数量上限约束。 |
| Q11 | 原始与最终指标可分别勾选；最高伤害的折前、折后为独立复选框，并提供预览。双选时显示 `最高伤害：x / x`，顺序固定为折前 / 折后。 |
| Q12 | 持久化完整原始伤害事件，保证来源、目标、直接来源和伤害类型任意组合的全历史精确查询。 |
| Q13 | 服务端负责排序、搜索与游标分页，每页固定 50 行。 |

Q14 与 Q15 的实例目录保留策略已经确定：

| 编号 | 问题 | 已确认结论 |
|---|---|---|
| Q14 | 已记录实例目录的淘汰策略 | 仅收录已记录实例；按 `instanceDirectoryRetentionHours` 的现实时间保留，默认 72 小时；每个世界全局最多 `instanceDirectoryLimit` 个候选，默认 5000；超额时淘汰最久未交互者；目录淘汰不得删除完整原始伤害事件。 |
| Q15 | 保留期计时基准 | 使用现实时间，单位为小时；不按游戏内 tick 或游戏内天数换算。 |

## 13. 实现后验收标准

以下实现已完成，后续修改不得破坏：

1. 完整事件存储采用二进制分段、段级索引、批量落盘、旧 JSONL 迁移、索引重建和尾帧损坏降级；封存段压缩以降低长期磁盘占用。
2. 焦点摘要有完整状态包与字段级增量包；登入和焦点切换建立一次历史缓存，后续匹配伤害在同一 tick 内增量聚合并发送一次。
3. 普通玩家的直接来源下钻由服务端通过完整事件历史证明，切换目标或关闭 GUI 时回收授权并恢复自身来源；断线时清除全部实例焦点状态。
4. 实例目录随聚合缓存持久化，并使用现实时间保留期和每世界数量上限清理；完整原始事件不受目录淘汰影响。

以下项目仍须完成验收：

1. 在单人、专用服务器、多人权限和不同界面尺寸下完成游戏内人工验收。
