# DamageStats 需求落差清单

> 本文件是对照 [REQUIREMENTS.md](REQUIREMENTS.md) 得出的未完成事项和未通过验收事项，只保留待处理内容。
>
> 维护规则：每一项完成代码修改并完成对应验证后，直接删除该项的整个小节；不得改写成“已完成”、打勾或长期保留历史记录。需求发生变化时，先修改 `REQUIREMENTS.md`，再同步调整本清单。已确认符合需求的内容不重复记录。
>
> 复核日期：2026-09-06。当前剩余事项仅为下列待处理内容。

## 一、确定的实现落差与设计缺陷

### 5. 低高度和不同分辨率下的布局适配没有完成

需求要求低高度窗口、超长名称和不同分辨率下不得重叠、越界，并对摘要卡片换行或分页。当前主界面使用固定的 `HEADER_HEIGHT = 164`、`FOOTER_HEIGHT = 32` 和固定按钮坐标，仅按宽度切换窄屏操作菜单，没有按可用高度重新布局或对摘要卡片分页。详情、历史和实体选择页也使用固定页头、页尾和行高，需要实际验收并补齐越界处理。

证据：`StatsScreen.java:32-35`、`StatsScreen.java:82-176`、`StatsDetailsScreen.java:22-45`、`StatsHistoryScreen.java:23-50`、`FocusEntitySelectorScreen.java:27-30`、`FocusEntitySelectorScreen.java:82-99`。

### 6. “清理全部记录”没有保证事件目录与聚合缓存跨文件原子一致

需求要求清理全部记录时原子清空事件日志、索引、聚合缓存和实例目录。当前流程先执行 `journal.clear()` 并重置内存，再调用 `StatsStorage.save()` 覆盖 `stats.json`；如果保存聚合缓存失败，旧的 `stats.json` 仍可能留在磁盘，而事件目录已经被清空。下次启动可能重新加载旧聚合缓存，造成清理后的数据复活。

证据：`ServerLifecycleHandler.java:121-135`、`StatsResetService.java:15-25`、`StatsStorage.java:58-64`、`StatsStorage.java:97-120`。

### 7. 正常关闭时忽略了刷新和封存失败结果，存在丢失待写事件的风险

`DamageEventJournal.close()` 调用 `flush()` 后不检查待写队列是否为空，也不检查 `sealActiveSegment()` 是否成功，随后无条件把 `closed` 设为 `true`。磁盘写入失败时，未落盘事件可能丢失且日志对象不再允许追加，不符合正常停服时可靠保存和失败可恢复的要求。

证据：`DamageEventJournal.java:184-204`、`ServerLifecycleHandler.java:97-109`。

### 8. 损坏的旧 JSONL 行会使整段迁移中止

迁移代码对合法 JSON 但字段不匹配的行可以记录错误后跳过，但 `JsonParser.parseString(line)` 遇到非法 JSON 时会抛出运行时异常，外层直接终止整个文件迁移并保留原文件。迁移策略没有做到坏行隔离后继续导入其余可恢复记录，需要明确并实现损坏行处理策略。

证据：`DamageEventJournal.java:433-475`，尤其是 `readLegacyEntries()` 中的 `JsonParser.parseString(line)`。

### 9. 聚合缓存损坏时没有从权威事件日志恢复实例目录和快速缓存

`StatsStorage.load()` 读取 `stats.json` 失败时直接返回空值，服务端虽然仍可从事件日志完成部分精确查询，但已持久化的实例目录、名称缓存和快速聚合不会从事件日志重建，实例选择器可能丢失历史实例，恢复后的运行状态也不完整。

证据：`StatsStorage.java:38-55`、`ServerLifecycleHandler.java:36-42`、`DamageTracker.java:161-179`。

### 10. 存在未使用的无参空构造方法

`OverlayPositionScreen` 同时提供带返回页面的构造方法和无参空构造方法，当前源码搜索不到无参构造方法的调用方。按照项目 Java 约束，应删除没有必要的空构造方法，并确认入口全部使用带返回页面的构造方法。

证据：`OverlayPositionScreen.java:88-97`。

## 二、尚未完成的人工验收与压测

### 11. 焦点会话超时状态的同步延迟尚未验收

焦点数据有伤害时会在同一服务端 tick 合并推送，但会话超时检查固定每 20 tick 执行一次。没有新伤害时，焦点从活跃变为非活跃最多可能延迟约 19 tick；需要在游戏内确认这是否满足“焦点数据更新瞬间同步”的要求，若不满足再调整超时状态维护方式。

证据：`SessionTickHandler.java:12-25`、`StatsSyncHandler.java:48-74`、`FocusSummaryCache.java:53-71`。

### 12. 必须完成游戏内场景验收和长期恢复压测

以下范围仍没有实机证据：

- 单人、专用服务端、多人权限场景，以及普通玩家直接来源下钻和关闭 GUI 后权限回收；
- 不同分辨率、低高度窗口、超长实体名称、第三方伤害类型 ID 的 GUI 和 Overlay 布局；
- 高频伤害、长期存档、实例目录 5000 条上限和 72 小时淘汰；
- 索引重建、活跃段尾帧损坏恢复、旧 JSONL 迁移、导出、三类存储清理和清理失败恢复；
- 大量历史数据下图表、历史、实例搜索和焦点切换的主线程响应时间与内存占用。

自动化测试不在当前需求范围内，但构建、`runData`（仅在翻译声明变化时）和上述人工验收/压测不能省略。
