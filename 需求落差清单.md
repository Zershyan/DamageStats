# DamageStats 需求落差清单

> 本文件是对照 [REQUIREMENTS.md](REQUIREMENTS.md) 得出的未完成事项和未通过验收事项，只保留待处理内容。
>
> 维护规则：每一项完成代码修改并完成对应验证后，直接删除该项的整个小节；不得改写成"已完成"、打勾或长期保留历史记录。需求发生变化时，先修改 `REQUIREMENTS.md`，再同步调整本清单。已确认符合需求的内容不重复记录。
>
> 复核日期：2026-09-07（已完成两轮全面复核）。当前剩余事项仅为下列待处理内容。

## 一、确定的实现落差与设计缺陷

*经两轮全面复核和修复，所有已识别的设计缺陷已修复完毕。*

**第一轮复核修复项**：
1. ✅ ServerStats 并发安全风险 - 已添加 volatile 修饰符
2. ✅ DamageEventJournal 锁竞争 - 已使用 ReadWriteLock 替换 synchronized
3. ✅ 实例目录淘汰逻辑 - 已保护玩家和有名称的实体
4. ✅ 焦点会话超时延迟 - 已改为每 tick 检查
5. ✅ 导出分块超时机制 - 已添加 10 秒超时保护
6. ✅ 下钻授权绕过风险 - 已在目标改变时重新验证
7. ✅ 超长实体名称防御 - 已降至 64 字符上限

**第二轮复核确认**：
- ✅ 无 TODO/FIXME/HACK 标记
- ✅ 无空 catch 块
- ✅ 无不当线程操作（Thread.sleep/wait/notify）
- ✅ 资源正确关闭（try-with-resources 或显式 close）
- ✅ 除零保护已到位（DPS 计算使用 Math.max(1, duration)）
- ✅ 网络协议版本控制正确（revision/requestId 机制）
- ✅ 无明显的嵌套集合性能问题
- ✅ 循环边界条件正确（while(true) 仅用于读取流，有 EOF 保护）

## 二、尚未完成的人工验收与压测

### 1. 必须完成游戏内场景验收和长期恢复压测

以下范围仍没有实机证据：

- 单人、专用服务端、多人权限场景，以及普通玩家直接来源下钻和关闭 GUI 后权限回收；
- 不同分辨率、低高度窗口、超长实体名称、第三方伤害类型 ID 的 GUI 和 Overlay 布局；
- 高频伤害、长期存档、实例目录 5000 条上限和 72 小时淘汰；
- 索引重建、活跃段尾帧损坏恢复、旧 JSONL 迁移、导出、三类存储清理和清理失败恢复；
- 大量历史数据下图表、历史、实例搜索和焦点切换的主线程响应时间与内存占用；
- ReadWriteLock 引入后的并发性能改善验证；
- 每 tick 会话检查的性能影响评估。

自动化测试不在当前需求范围内，但构建、`runData`（仅在翻译声明变化时）和上述人工验收/压测不能省略。
